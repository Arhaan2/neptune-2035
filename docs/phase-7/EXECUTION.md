# Phase 7 execution record

2026-09-11: The user's open Phase 1 worktree and two untracked files were preserved. A fresh isolated clone explicitly checked out origin/main (the remote default points to an older development branch). Main is Phase 6 merge 9db8565, Pages ce643245. Read PR #6 and release phase-6-2026-09-11; supplied counts are baseline claims only.

Actual native agents initially run concurrently: `/root/building`, `/root/testing`, `/root/verification`, alongside `/root`. Runtime supports three worker slots; `/root/fixing` will take a returned initial-review slot and remain a distinct repair agent. Worktrees: `/private/tmp/neptune-phase7-{building,testing,verification}`; integration `/private/tmp/neptune-phase7`. Evidence lives outside source at `/private/tmp/neptune-phase7-evidence`. Only root integrates/pushes/releases.

C0: presentation contract and requirement matrix established before feature edits. Building proposed separate history worker and exact boundary observer. Testing independently identified existing browser inventory/exclusions and required observables. Verification independently flagged mutable active replay and fractional transfer boundaries as primary risks. Callback is limited to snapshot-copy access; no numerical semantics change authorized.

C0 independently approved by `/root/verification` at 170d9be/tree4954a22e (native receipt external). Testing independently expanded Vitest inventory to 784 cases/45 files; ordinary browser collection is180 scheduled/11 files, supported acceptance81/6 files with exactly two historical large-campus Chromium/WebKit exclusions. These were collection results, not test passes. Fresh baseline public verification matched89 served files plus2 committed markers, including45 preview files. Pages API confirms legacy source `codex/pages` `/`.

Distinct `/root/fixing` spawned after initial Verification returned. It identified P7-D01, stale campaign inspection mixing historical labels with the current draft plan. Exclusive DecisionPanel.tsx ownership transferred from Building to Fixing; Testing resumed concurrently for the original failing UI reproduction while Building continued C1 then C2. This is actual overlapping agent work.

C1 Building checkpoint8d2f520 integrated as e110ee5. Own checks typecheck/lint passed; browser/independent acceptance remains pending. Added connected asset presentation, workspace guidance, global/fallback failed-state projection independent from selection and bounded camera framing cache. Building moved to C2 while Testing exercises C1 and D01.

C1 was independently challenged with actual canvas/HTML pump and failed global asset journeys. D01/D02 were repaired in the separate lane and both received independent semantic closure. C2da1e019 integrated4637f07 adds a separate bounded history worker and canonical observer copies; explicit metric-marker bracketing fa9b1e9 integratedae99165. Verification independently compared true fractional transfer boundaries and confirmed raw recovery-marker time is not itself an invented physical snapshot. Testing's D03 strict identity regression and native D04/D05 failures drove focused repairs while Building completed C3.

C3 Building4f328ea integrated472b145: authoritative decision explanations and actual stepwise walkthrough. Testing added41 focused unit cases and13 browser declarations. Its pinned Chromium C3 run at09c5cb7 passed11 and failed only the two original D06 provenance reproductions, with screenshots/traces retained; all41 focused units plus typecheck/lint passed. This is checkpoint evidence, not full release acceptance. Fixing independently reran D06 originals unchanged after the focused repair and returned2 passes. Final exact-source testing and Verification remain separate.

Building finished and handed all production ownership back. The runtime temporarily rejected reactivating Verification with an agent-thread-limit error while Testing/Fixing were active; after Testing returned its slot, the actual `/root/verification` resumed successfully on15836e4. No role was impersonated or substituted. Root continues documentation, gate and recording tooling while Verification independently challenges C3 and Fixing handles concrete defects. Final gates and remote release operations serialize heavy resources.

Verification's C3 review rejected15836e4 for D08 (original narrative over changed execution) and D09 (missing exact legacy report qualification), after ordinary primary and three alternate walkthroughs passed. Separate Fixing commits af73752/d87eda1 were integrated694daf5/1060978; Testing first recorded both original failures, then passed the unchanged browser/unit regressions against the matching1060978 production/test tree. D06/D07 originals also passed independently. A stopped Testing server caused three earlier connection-refused navigation failures; logs were retained and the same cases reran only after observable server readiness. No feature assertion ran in that failed environment attempt.

Full pre-freeze browser collection is225 scheduled cases across12 files, including45 new Phase7 positions (15 declarations in three engines). The supported release gate retains the original81 positions plus these45:126 scheduled, with only the two exact historical exclusions. Collection does not prove passage. Final counts, receipts, candidate identity and independent decisions remain outside frozen source.

First full frozen candidate c9c5229 was rejected: clean installation, typecheck, lint, all826 units including both real telemetry integrations and production build passed; browsers completed121 passes,3 Phase3 qualifier-locator failures and2 historical exclusions. Independent artifact review also rejected extra macOS metadata in the tar while confirming the actual tested dist was clean. Separate Fixing H01/H02 commits correct archive metadata and scope the same exact banner assertion; no application byte is changed. The original gate, archive, screenshots and traces remain preserved. A new source/tree and fresh full gate are mandatory; this first run is never represented as final acceptance.
