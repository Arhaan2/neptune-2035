# Independent topology/allocation slice review

Reviewer `/root/verification` inspected immutable root `f171adc`, tree `98b294dee443f071dab34cbd1d1d4f296eb286ba` (production `5ecc557` plus repair `176e3e0`). The review worktree merged that candidate and independently confirmed identical tree content. This slice includes the new transfer API/controller/topology/design files and their catalog/design-validator hooks; engine dispatch/persistence integration is pending and receives no acceptance here.

PH5-T01 is **CLOSED** for this slice. Inspected native Fixing before/after logs: before, the unchanged missing/duplicate-resource assertion failed because the duplicate path did not throw (23 passed, 1 failed); after, 32 passed in two files. Inspected the actual focused repair, which rejects duplicate resource IDs before path mapping or any debit. Independently reran all 32 allocation/oracle tests in the review worktree: two files passed, zero failures/skips/retries, 225 ms. A separate reviewer API probe rejected `['shared','shared']` with `TRANSFER_RESOURCE_PATH_DUPLICATE`. Normal 100 kW source/60 kW native/two 30 kW requests admitted stable ID `a`, refused `b`, and retained 10 kW. A refusal does not debit other resources, and each invocation initializes a detached usage ledger.

PH5-T02 is independently **CONFIRMED OPEN** against this candidate, consistent with Testing's separate role-binding finding. `transfer/design.ts:44-51` checks asset existence/owner but does not bind `originalFeederId` to the original electrical path or an eligible feeder role. The reviewer substituted `platform-002/cluster` (a same-owner network switch), recomputed design revision, and `validateDesign` accepted it. Its healthy native power path did not include that trigger. On a trip of that network switch, `updateTransfer` nevertheless reported `FEEDER_FAULT`, isolated the healthy original electrical supply and entered `WAITING` at 5 s with deadline 7.375 s. The physical path became unsupported after isolation. Required fix: bind the declared feeder, isolator, receiving bus, donor and ties to their actual declared electrical relationships/roles; reject malformed aliases at design admission. Keep regressions for same-owner tie/isolator/bus and a same-owner non-power-path asset. This is an API/controller reproduction, not an engine or browser result.

Other source review conclusions: candidate ordering is explicit priority then stable ID; the pure allocator preserves indivisibility and fresh reservations; active projection changes connection positions without reparenting physical assets; one-hop donor exclusions and native radial checks are present. Native requested watts may exceed a rating in a valid inadequate design: the allocator reports zero headroom and refuses transfer, so the engine/report must distinguish required native watts from delivered native allocation. Complete active-path power, conversion/losses, charging, network coupling, timer interval accounting and imported transfer-state validation remain pending engine-slice checks V5-C01–C08, not alleged defects in this unfinished slice.

Durable native evidence in the original workspace:

- `artifacts/phase-5-20260911/verification/f171adc-allocator-tests.log`
- `artifacts/phase-5-20260911/verification/slice1-probes.mjs`
- `artifacts/phase-5-20260911/verification/f171adc-probes.json`
- Reviewed separately: `artifacts/phase-5-20260911/fixing/PH5-T01-before.log` and `PH5-T01-after.log`.

No production/tests were edited, and no remote action or release sign-off was performed. Returning idle permits the distinct Fixing identity to receive the completed design-validation surface.
