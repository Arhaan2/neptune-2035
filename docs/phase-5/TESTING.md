# Independent Phase 5 testing evidence

Testing agent `/root/testing` uses branch `codex/neptune-phase5-testing`, isolated worktree `/private/tmp/neptune-phase5-testing`, baseline `1f7f16c4680d2839af8775fe75b96d3f2ce77cfa`. It owns Phase 5 tests, fixtures and these testing receipts. Production source is integrated only from named immutable Building/Root commits. It never changes production code, accepted earlier tests, timeout budgets, retries or exclusions. Raw native evidence is durable under `/Users/arhaan/Documents/ChatGPT/Neptune/artifacts/phase-5-20260911/testing/`.

## Recorded checks

| Source | Check | Result | Native evidence |
| --- | --- | --- | --- |
| `1f7f16c` | Baseline Phase4 engine/metrics/persistence, existing physics/equipment propagation | 108 PASS, 0 FAIL, 0 pending, five files | `baseline-focused.json` |
| `1f7f16c` plus independent oracle tests committed as `dae3f23` | Frozen capacity and piecewise service arithmetic; existing whole-run evaluator checked against independent history | 8 PASS, 0 FAIL, 0 pending, one file | `frozen-oracles.json` |
| `5ecc557` | Independent allocator plus oracles | 31 PASS / 1 FAIL: duplicate path resource accepted | `slice-5ecc557-allocation.json` |
| `176e3e0` | Duplicate-path repair | All32 allocator/oracle checks PASS; added topology checks exposed malformed feeder roles | `slice-176e3e0-topology-allocation.json`, `slice-176e3e0-role-binding.json` |
| `a9d15ad` | Real engine feeder/policy/path/fault sequence and worker execution | Engine20 PASS; supported worker chunks9 PASS | `slice-a9d15ad-engine-valid-events.json`, `slice-a9d15ad-worker-supported-chunks.json` |
| `38ab672` | Distinct hardware/actual original-feeder binding repair | All34 topology checks PASS; initial capacity evaluation still failed separately | `slice-38ab672-initial-eligibility.json` |
| `9ebec1e` | Initial headroom evaluation, real shared donor partial restoration, native-load protection and thermal limitation | Engine/coupling27 PASS | `slice-9ebec1e-coupling.json` |
| `728bc0a` | Complete checkpoint binding repair | Persistence/engine/worker65 PASS; full Phase5 set136 PASS / 2 FAIL, both installed standby demand | `slice-728bc0a-checkpoint-retest.json`, `slice-728bc0a-all-phase5.json` |
| `728bc0a` with test `5c7187b` | Delay0/0.125/0.5/2.375/10/60s including terminal/pending cases | Engine29 PASS | `slice-728bc0a-delay-boundaries.json` |
| `a79f005` with T02/V01 | Production Chromium UI, four real-worker/export/replay journeys | 4 PASS, 0 FAIL, 0 skipped, 0 retries,5.0s | `browser-a79f005-chromium-authorized.json`; decoded `native-ui-exports/*.json` |
| `7ee2153` with all prior repairs and latest independent tests | Complete independent Phase5 unit/integration set | **146 PASS, 0 FAIL, 0 pending, seven files**; production build/typecheck/lint also PASS | `slice-7ee2153-all-phase5.json` |
| `7ee2153` repaired production build | Four actual Chromium UI/worker journeys, complete numerical export/import/replay and mobile fallback | **4 PASS, 0 FAIL, 0 skipped, 0 retries,5.1s**; no console/page errors or failed asset/worker requests | `browser-7ee2153-chromium.json`; decoded `native-ui-exports-7ee2153/*.json` |
| `7493527` | Three additional exported checkpoint semantic corruption cases | 33 PASS / 3 FAIL: forged early closure, blocked unserved watts, and contradictory isolation reason accepted | `slice-7493527-semantic-corruption.json` |
| `f9dbfcc` | Semantic checkpoint follow-up repair | Persistence/engine/worker **74 PASS**, including all three new regressions and valid evolving-demand histories | `slice-f9dbfcc-semantic-retest.json` |
| `7493527` | Additional valid donor conversion,63kW/65kW ties | Coupling9 PASS / 2 FAIL: both valid designs failed to commit at closure | `slice-7493527-extra-conversion.json` |
| `f9dbfcc` | Energized donor-to-source loop admission | Topology34 PASS / 1 FAIL: source backfeed loop accepted | `slice-f9dbfcc-source-loop.json` |
| `b56e12c`, including `f655ec9` and `f9dbfcc` | Final complete independent Phase5 set after semantic/conversion/source-loop repairs | **152 PASS, 0 FAIL, 0 pending, seven files**; typecheck/lint PASS | `slice-b56e12c-all-phase5.json` |
| `d1e7004` production with new regression `272be2c` | Explicit Compare/Operate draft preservation in WebKit | **1 FAIL**: selected healthy becomes reference after returning to Operate | `PH5-L01/baseline-webkit.json`, retained trace |
| `6c19ac2` production repair with unchanged regression `272be2c` | Same WebKit journey and actual healthy project export | **1 PASS, 0 FAIL, 0 skipped, 0 retries,1.6s**; duration40s, dwell7s, no fault inputs, zero shortfall/service violation, real worker with no console/page errors or failed requests | `PH5-L01/repaired-webkit.json`, `PH5-L01/repaired-native-project-export.json` |

The baseline focused invocation was `npm test -- tests/phase4-engine.test.ts tests/phase4-metrics.test.ts tests/phase4-persistence.test.ts tests/twin-physics.test.ts tests/phase2-propagation.test.ts --reporter=json --outputFile=.../baseline-focused.json`. Tests used the original installed dependencies through a worktree symlink; the release owner's required clean dependency install and final complete gates are separate acceptance evidence. These focused passes make no claim about an untested Phase 5 candidate.

## Requirement coverage ledger

| Requirement | Independent tests/evidence | Status |
| --- | --- | --- |
| A eligible feeder interruption and recovery; own unfaulted baselines | `phase5-oracles.test.ts`, `phase5-engine.test.ts`, eligible UI journey;80 vs19 accelerator-s, onset4.375/confirmation9.375 | Passed focused engine/Chromium |
| B same III hardware, disabled controller | Topology test asserts identical installed assets; engine verifies80 accelerator-s and DISABLED refusal | Passed focused |
| C receiving bus/common upstream/tie/donor no-benefit, unrelated-domain protection | Engine refusal cases; actual bus UI export80 vs80 and healthy unrelated8+8 | Passed focused/Chromium |
| D zero/insufficient/exact/sufficient, partial, tie and upstream bottlenecks | `phase5-capacity.test.ts`, independent exhaustive-subset oracle,36 bounded multi-resource combinations (3 native loads × 4 source ratings × 3 tie ratings); real shared donor partial99; UI140kW bottleneck99 vs160; installed standby recipient63/64kW and native donor125/126kW; additional donor conversion63/65kW boundaries | Passed repaired combined; core demonstration Chromium passed |
| E competing priorities, native protection, stable-order allocation, no reservation duplication | Allocator exhaustive priorities/permutations; real equal-priority route/module/asset/edge permutation and native-demand protection in `phase5-coupling.test.ts` | Passed repaired combined |
| F fault clearance, delay invalidation, downstream/post-transfer failure, demand shedding | `phase5-engine.test.ts` exact fault/deadline order, cancellation, non-reversion, single attempt; `phase5-coupling.test.ts` demand increase before/after closure | Passed focused |
| G before/during/after checkpoint, replay/export/import, chunks, worker cancel/restart, new runs and structural staleness | `phase5-persistence.test.ts` including malformed snapshot and semantic history rejection, actual partial-charge battery continuity and changed demand; `phase5-worker.test.ts`; UI project/import/replay/stale historical report | Passed T03 and semantic follow-up combined; core Chromium passed |
| H malformed/unsupported topology/ratings/timing, thermal/network coupling | Typed failure classification in topology/allocator, including energized donor-to-source loop rejection;29 engine tests including delay endpoints; real thermal case3x160nodes/pumpSpeed0/600s retains transferred supply while thermal controller curtails service; required network failure remains unserviceable | Passed repaired combined |
| Existing accepted gates and two actual telemetry integrations | Original activations verified `NEPTUNE_TELEMETRY_BROWSER=1` and `NEPTUNE_TELEMETRY_APP=1` | Final complete gates owned by Root; pending |
| Every-browser real UI numerical export and worker/request evidence | `tests/browser/phase5.spec.ts`, four journeys run without retries; complete numeric exports and empty error/failed-request collectors | Repaired Chromium passed; release-owner complete three-browser/CI/hosted gates are separate receipts |

## Failure provenance and harness corrections

No failed evidence is discarded. Initial browser invocation failed4 launches before any page action because the sandbox denied Chromium's MachPortRendezvous; the ordinary approved escalation then ran the same build and all4 journeys passed. This is an environment launch failure, not a flaky product test or a retry allowance. Unit fixture harness corrections were limited to reading catalog `evidence`/`assumptions` in their actual fields instead of the provenance reference ID, encoding event IDs with the existing accepted stable alphabet, and using the existing10s maximum worker chunk instead of invalid12s. Corresponding initial native failures remain retained. No production behavior, earlier assertion, numerical tolerance, timeout, retry count or exclusion was weakened.

T01 duplicate resource validation, T02 feeder-role binding, V01 initial headroom evaluation, T03 checkpoint binding and V02 installed standby demand are independently CLOSED on the repaired source. The retained pre-repair V02 evidence shows that with supported `pump-physical` standby and a failed duty pump,63000W tie incorrectly reported transferred with no unserved load;64000W tie served8 but admitted62080.096378W while actual module plus platform network required63632.533679W. Repair7ee2153 prospectively accounts for the installed available standby. Independent tests now require63000W refusal,64000W restoration with sufficient reserved watts,125000W shared donor refusal while protecting native8, and126000W shared donor restoration with no overspend. All pass without changing expected capacity or tolerances.

Testing's final production preview on port5186 was stopped after the repaired browser receipt. No heavyweight matrix is running and no production file was written by Testing. The complete final clean-install/telemetry/all-browser/source-CI/public release gates remain the release owner's responsibility; this focused receipt does not assert they have already run.

Independent Verification subsequently exposed three remaining T03 semantic holes, V03 proposed-path conversion loss inconsistency and V04 source-loop admission. Testing reproduced each on immutable source before its repair and retained all failing native reports. The final152-test pass independently closes them: closure history cannot predate its configured deadline; blocked unserved evidence cannot be forged; a closed transfer cannot claim unconfirmed isolation; an additional declared donor transformer requires63343.229209W so a63kW tie refuses and65kW restores8 accelerators; and enabled power backfeed into the sole source is rejected as unsupported at design admission. Conversion tests also resume the t3 checkpoint and compare the complete result with continuous execution. Historical refusal values remain tied to their recorded decision while valid later workload changes continue to roundtrip. No expected value, tolerance, timeout or exclusion was weakened.

Final test ownership is released after this receipt; no testing server or matrix remains active. The final product gate, including the release owner's last-decision explanatory UI copy, runs separately on the frozen integration candidate.

## Full-gate follow-up: experiment draft preservation

The release owner's first full local gate on `d1e7004` passed665 unit/integration tests but exposed an actual product failure in the retained WebKit unequal-observation-window journey (48 browser passes,1 failure,2 existing skips). Testing independently extracted the recorded definition and inputs from that native failure trace: the requested healthy second run had become the reference network interruption, with an8-accelerator service outage from5 to15 seconds, no thermal violation, and therefore an exact80 accelerator-second loss. `PH5-L01/trace-recorded-definition.json` and `PH5-L01/diagnosis.json` preserve that evidence and identify it as trace-derived, not a native project export.

`ExperimentPanel` held draft fields in local React state while `TwinApp` unmounted the panel in Compare. Returning to Operate recreated the default reference preset. Testing added a separate browser regression in `272be2c` that first observes Compare visible and the whole-experiment panel hidden, then requires healthy,40s duration and7s dwell to survive returning to Operate. It reproducibly fails on the immutable baseline with expected `healthy`, actual `reference`. Fixing's source-only `6c19ac2` keeps the panel mounted and hidden across view changes. The unchanged test then passes in WebKit and exports the actual healthy40s worker result with an empty disturbance list and zero accumulated service loss. The old Phase4 test, all earlier assertions, timeouts, retries and exclusions remain unchanged. This closes PH5-L01 independently; the release owner will repeat the complete final gate. Testing's preview on5186 is stopped and test/document ownership is released.
