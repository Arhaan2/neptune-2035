# Independent Phase 5 testing contract

Frozen against baseline `1f7f16c4680d2839af8775fe75b96d3f2ce77cfa`, before Building supplied any candidate. Testing owns Phase 5 tests/fixtures/browser journeys only; no production behavior is changed to satisfy an expectation.

The independent capacity oracle enumerates all feasible subsets of a bounded synthetic fixture, then chooses the subset that lexicographically protects declared priority and stable asset IDs. A 100,000 W shared resource, 60,000 W native load and two indivisible 30,000 W recipients admits recipient-a only and retains 10,000 W spare. These are test numbers, not equipment specifications. Every resource on a bundle's path counts that physical load exactly once. Ratings 60,000/89,999/90,000/100,000/120,000 W establish zero/insufficient/exact/partial/sufficient expectations before candidate inspection. A single indivisible oversized bundle must remain unserved.

The independent service oracle computes the area of declared piecewise shortfall intervals without invoking the production experiment evaluator. Demand24 comprises healthy native16 and affected8, fault2 s, transfer4.375 s, horizon12 s, thermal300 K, no batteries, no startup or other service loss, required healthy network, and recovery dwell5 s. II loses80 accelerator-seconds; III loses19 accelerator-seconds across2.375 s. Recovery onset4.375 and confirmation9.375 s are different events. The partial fixture has native8 and two affected8 bundles: one restored at4.375 s leaves99 accelerator-seconds unmet and never satisfies complete-demand recovery. Tests exercise the existing evaluator against these independent answers; coupled engine demonstrations receive their own observed values, not forced synthetic answers.

Absolute tolerances are frozen in `tests/fixtures/phase-5/frozen-expectations.json`: synthetic resource arithmetic1e-7 W, time and service integrals1e-8, coupled resource accounting1e-6 W, checkpoint temperature1e-9 K, checkpoint energy1e-8 Wh. Integer counts, IDs, booleans, reason codes, switch positions, allocation membership and JSON roundtrips require exact equality. These tolerances allow IEEE754 accumulation noise only; they cannot conceal one node, one interval, or one switched bundle. Exact dyadic timing fixtures require exact values where arithmetic permits. A thermal limitation test asserts loss of useful service despite restored electrical state, without prescribing a synthetic temperature trajectory.

Mandatory coverage remains A eligible III benefit and own unfaulted baselines; B disabled controller; C bus/source/tie/donor refusals; D all headroom boundaries/partial/tie/shared; E competing stable priority and native protection; F cancellation/revalidation/post-transfer shedding; G complete checkpoints/replay/worker lifecycle/revision boundaries; H invalid references/loops/ratings/timing and thermal/network coupling. Required browser journeys exercise eligible, no-benefit, partial/shared, export/import/replay through actual UI/workers in Chromium, Firefox and WebKit. Native JSON exports and error/request observations are retained alongside screenshots. Existing60 s browser budgets,12 s assertion budgets and zero retries remain unchanged.

The original telemetry integration activations are `NEPTUNE_TELEMETRY_BROWSER=1` and `NEPTUNE_TELEMETRY_APP=1` in `tests/twin-telemetry.test.ts`; both must run during final complete gates. Root coordinates heavyweight matrices and owns clean dependency installation, source CI and hosted release gates. No new exclusions are authorized.
