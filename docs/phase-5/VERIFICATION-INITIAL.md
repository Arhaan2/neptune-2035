# Phase 5 independent initial contract review

Reviewer `/root/verification`; baseline `1f7f16c4680d2839af8775fe75b96d3f2ce77cfa`, tree `6928aeb11bcf53c3b6d6311fe67920d81e110e7f`; frozen Building contract `68b19b1`. This is an initial source/contract review and independently executed arithmetic, not implementation or release acceptance.

Read the complete Phase 5 request, applicable AGENTS, v2 architecture/readiness, Phase 3 equipment and Phase 4 contract/review material, actual asset/catalog/electrical/network-power/network paths, engine event/step loop, project/state validation, experiment definition/runtime/validation and worker boundaries. No standalone roadmap file was discoverable by repository filename search; the supplied request states the Phase 5 scope. No product or test file was edited. Publication/baseline HTTP findings belong to root and are not represented as reviewer-run checks here.

The contract's explicit whole-platform, one-hop topology; positive isolation; one attempt per run; non-reversion; native-first atomic admission; and conservative shedding provide a coherent bounded capability. Whole-platform partial restoration means separate recipient platforms, so the partial/competition acceptance fixture needs at least two eligible recipients. The following are implementation hazards requiring later evidence, not reproduced Phase 5 defects.

| ID | Source in baseline | Required implementation/review closure |
| --- | --- | --- |
| V5-C01 | `engine/simulation.ts:25-40,138-153,305`; `solvers/network-power.ts:7-37`; `solvers/network.ts:162,175`; `catalog/equipment.ts:176-184` | Active supply changes must reach module ancestry, actual conversion efficiencies, root/platform switch power, the network evaluator's nested electrical check, and summary power. Keep physical parent/module IDs unchanged. Static cached topology cannot override transferred state. |
| V5-C02 | `solvers/electrical.ts:76-99`; `engine/simulation.ts:139-164`; `solvers/network-power.ts:30-35` | Existing proportional allocation only constrains a domain and common supply. Minimum ancestor rating applied independently to two domains does not enforce a shared intermediate source/transformer. New native-first admission needs one ledger per actual shared resource, including switch loads, losses, tie and receiving equipment; charging comes last. Verify watts at the same resource-side convention. |
| V5-C03 | `engine/simulation.ts:50-73,285-295` | Current code resolves after each same-time event. Transfer switching must wait until all admitted same-time faults/restores have applied, then run deadline/controller allocation in stable priority/ID order. Recipient fault preceding donor fault at a closure boundary cannot transiently close a tie. |
| V5-C04 | `engine/simulation.ts:269-290`; `persistence/state.ts:19-22`; `experiment/validation.ts:20,43` | Split deadlines change more than dt: checkpoint integer-time/macrostep positioning and `committedIntervals*integrationStepS===elapsedS` presently assume a fixed grid. Version/bind transfer timing semantics while preserving legacy checks, exact duration metrics and atomic rejected candidates. Test 0.125-second deadlines under a 1-second ordinary step, before/at/after faults and chunk equivalence. |
| V5-C05 | `catalog/equipment.ts:159-168`; `persistence/project.ts:62,88,104`; `experiment/definition.ts:26,46`; `assets/design.ts:166-168` | The proposed optional extension/new model with unchanged legacy solver is coherent only if every identity/export/import/recalculation/experiment gate selects the model from the actual design. Unknown extension/policy versions must be rejected; explicit legacy recalculation must not silently install transfer. Adding transfer fields only to state identity is insufficient. |
| V5-C06 | `engine/simulation.ts:123-164`; `solvers/electrical.ts:51-70` | Admission must assess full restored bundle demand, not current near-zero power, idle network-blocked draw, thermally curtailed power or battery output. UPS finite energy remains continuous and must not supply invented spare capacity. Exact-boundary cases must include grid conversion and auxiliaries. Useful service still comes from coupled solver results. |
| V5-C07 | `assets/design.ts:62-77,99-102`; `experiment/definition.ts:83`; Phase 4 contract comparison section | Declare separate original-feeder and receiving-bus identities, and map II/III faults by the same physical role/load. Existing III `segment-feeder` is both the power-domain bus and static upstream endpoint. Splitting it must not turn a receiving-bus fault into a transferable feeder fault. Compare common workload/environment/initial procedure and publish each actual initial snapshot, transfer disabled control, own unfaulted baseline and added hardware/cost scope. |
| V5-C08 | `persistence/state.ts:29-59,83-95`; `engine/simulation.ts:277`; `experiment/runtime.ts:17-18` | Clone/validate complete transfer state atomically, including pending deadline, original/tie positions, active allocations, attempt/transition sequence and aggregate history. Resume must reject inconsistent positions/history and cannot mutate input checkpoints or duplicate actions. Current event fault validation is the standard to retain. |

Independent arithmetic was executed with Python rational arithmetic and no production imports. Raw JSON is retained outside disposable worktrees at `artifacts/phase-5-20260911/verification/initial-independent-oracles.json` in the original workspace.

| Fixture | Independently expected |
| --- | --- |
| 100,000 W shared, 60,000 W native, two equal-priority 30,000 W bundles | Admit stable `recipient-001`, refuse `recipient-002`, 10,000 W spare. |
| Required16, fault5 s, full transfer7.5 s, horizon20 s | II shortfall240 accelerator-seconds and15 s interruption; III shortfall40 and2.5 s interruption. Recovery onset7.5, confirmation12.5 for5 s dwell. |
| Required16, two8-accelerator bundles restore7.5 s and12 s | Shortfall16×2.5 +8×4.5 =76 accelerator-seconds; full-demand interruption7 s; recovery onset12, confirmation17 for5 s dwell. |

The service fixtures assume constant required inventory and no UPS ride-through, cooling/network/startup loss or repair. They are isolated evaluator oracles, not predicted coupled demonstrations. Exact rational arithmetic defines expected values before candidate output; an absolute `1e-7` W/accelerator-second tolerance is proposed only for these isolated scalar fixtures. Coupled demonstrations need their own declared assumptions and evidence. The raw output explicitly distinguishes all of these limits.

Initial review result: contract direction accepted for implementation; V5-C01–C08 remain review checkpoints. No implemented capability or release sign-off is claimed. This identity returns for independent actual-candidate and final native-evidence review after the Fixing slot rotation.
