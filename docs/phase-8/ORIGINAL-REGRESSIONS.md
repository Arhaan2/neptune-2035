# Phase 8 original finding regression ledger

The original register is `docs/phase-0/ISSUES.md`, pinned to source `23de38a50cf702bbe1e0f0bf56f61c0b5af4a78f`. Phase 8 uses the unchanged `scripts/phase-0/clean-check.mjs --diagnostics-only` to extract that exact Git archive, install its own lockfile, and overlay only the original diagnostic tooling and fixtures. The harness checks every original source file before importing its real solvers. It never applies current production code to the old baseline.

The fresh Phase 8 historical run on 11 September 2026 reproduced all six registered observations. Original dependency installation and the six-finding audit exited 0. Both raw desired-behavior commands exited 1 for assertion failures about actual returned behavior; neither was an import, compilation, missing-API, or dependency failure. All 216 preserved source files and the lockfile stayed unchanged. Native results belong in the external candidate evidence bundle, identified by `receipt.json` and `original/execution.json`, rather than in production payloads.

| Original ID | Honest classification | Native original witness | Current regression / disposition |
| --- | --- | --- | --- |
| PH0-001 | Defect corrected in Phase 1 | In-memory infinite shutoff returns infinite residual; infinite free flow reaches an unrelated Reynolds-number error. Two explicit-invalid-rating assertions fail. | `tests/phase8-original-regressions.test.ts` preserves the exact finite control and invalid input, requires typed invalid-input rejection for the exact rating field. Existing `phase1-numerical` also covers every malformed scalar and stopped circuit. |
| PH0-002 | Defect corrected in Phase 1 | The engine exports 1,001 applied commands which its importer rejects; valid scheduled 86,401-second and 30-day events reject; a genuinely integrated 86,401-second clock rejects. Four semantic assertions fail. | `phase8-original-regressions` preserves simultaneous command inputs, scheduled boundaries, complete restore and continuation; `phase1-persistence` retains genuine 86,399/86,400/86,401-second integration, complete checkpoints, 10,000-event contract and continued trajectory equality. |
| PH0-003 | Supported inadequacy retained; subsequent networking capability added | The 4,001-node design exceeds a shared 400 Gbit/s port under assumed 100 Mbit/s/node demand. This was correctly reported, not bad arithmetic. | `phase1-deferred` retains the 4,000/4,001-node capacity boundary; Phase 3 network tests independently cover configured alternatives. Large legacy-network configurations may be valid but inadequate. |
| PH0-004 | Equipment ownership risk resolved; no original incorrect numerical result established | Inspector and solver coincidentally used identical pump defaults; a test-local half-shutoff counterfactual demonstrated sensitivity, not a supported runtime edit. | `phase1-deferred` and `phase2-propagation` require installed specification changes to reach affected solver outputs and retain unchanged flow for efficiency-only replacement. This is ownership repair, not a historical arithmetic defect. |
| PH0-005 | Whole-run evaluation capability added in Phase 4 | Five seconds without service recover before a healthy endpoint and passing endpoint sizing assessment. Observer confirms the interruption and saved events remain present. | `phase1-deferred` retains this real endpoint-only witness. `phase4-metrics` and `phase4-engine` require accumulated service loss, extrema, duration, recovery dwell and incomplete/not-recovered outcomes. Endpoint sizing remains an explicitly separate assessment. |
| PH0-006 | Bounded automatic transfer capability added in Phase 5 | Matched Gen II/III feeder outage loses service while original ties are disabled; an unsupported close-tie command is explicitly rejected. | `phase1-deferred` retains original radial behavior; `phase5-engine` and frozen independent oracles require eligible modeled benefit and refusal for receiving-bus/common-source faults. No arbitrary meshed topology or independent common-source supply is claimed. |

Reproduce the original and current regression evidence from a clean immutable candidate with installed dependencies:

```sh
node scripts/phase-8-history/reproduce.mjs --out=/absolute/new/evidence-directory
```

The output directory must be new. The runner records current commit/tree, exact commands, native exit statuses and log hashes, then checks source immutability. A failed assertion remains a failure. Historical expected-failure evidence and corrected candidate PASS are separately represented. The complete candidate gate additionally runs the complete retained unit and browser suites; this bounded historical wrapper is not a substitute.

The two retained browser exclusions are exactly `prototype.spec.ts` → `public prototype large-campus functional Step 10s` for Chromium and WebKit. The original reason is “One focused large-campus check on the previously affected browser.” The Firefox position remains active. Phase 8 adds a separate one-second largest-enabled-campus worker boundary on all three engines; it does not claim that this proves the excluded ten-second workflows. Correctness, memory observations, and runtime budgets remain separately stated in the operating-envelope evidence.

Simulated, design-stage prototype; physical validation pending.
