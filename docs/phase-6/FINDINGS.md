# Phase 6 single defect queue

Application defects and demonstrated environment/harness failures are recorded separately. No author closes their own implementation as independently verified. Native failed output is retained outside the frozen source tree.

| ID | Severity / class | Affected commit | Reproduction / expected vs actual | Owner | Fix commit | Retest | Independent closure |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P6-T001 | Blocker / contract | ee40a70 | Delete any of four numeric requirements; expected rejection, accepted instead. | Fixing, contract.ts lease | 0f47b75 | Testing 42/42 unchanged assertions; 74-field omission audit | CLOSED by Testing; contract-T001-retest logs |
| P6-T002 | Blocker / missing evidence | 379727d | Canonical unavailable terminal boundary with zero unavailable interval duration; expected unresolved, produced feasible/recommended. | Building | cae0df34 | Fixing boundary probe and Testing 32/32 regression retest passed | CLOSED by Testing; evaluator-cae0df3 logs |
| P6-T003 | Blocker / worker lifecycle | cae0df34 | Worker never replies; no outer response deadline, campaign hangs. | Fixing, worker-client.ts lease | 9d96140 | Testing 17/17 runner/lifecycle regressions passed | CLOSED independently by Testing |
| P6-T004 | Blocker / incomplete scope | cae0df34 | Central no-feasible result with sensitivity cells pending retained scopeComplete=true. | Fixing, runner.ts lease | 9d96140 | Testing unchanged partial-sensitivity assertion passed | CLOSED independently by Testing |

Initial environment observations: the supplied workspace is an older Phase 1 worktree with two unrelated untracked files. A read-only inventory located accepted Phase 5; isolated Phase 6 worktrees were created without changing those files. Sandboxed network access cannot resolve GitHub; authorized escalated read-only fetch succeeds. CLI `gh auth status` in the sandbox reports invalid authentication; the configured GitHub connector successfully reads the current repository. No release blocker is inferred until actual publication capability is checked.

Environment/harness provenance: sandbox listeners produced EPERM in baseline SSE/Vite checks; authorized listeners succeeded. One baseline telemetry application test failed because its required origin is port 5173 while the isolated test server used 5177. Starting the actual unchanged application at its required port yielded both real telemetry integrations passing (47/47 telemetry tests). No assertion, timeout or application code was relaxed. CLI GitHub authentication works outside the network sandbox; its earlier sandbox status was not an authentication blocker. Raw failed runs are retained.

| Additional ID | Severity / affected commit | Reproduction | Repair | Independent closure |
| --- | --- | --- | --- | --- |
| P6-T005 | Blocker / UI14f9fb9 with evidencef1be93 | Actual III candidate load emits duplicate React cost-row keys; first browser run4passes/1failure despite correct numerical result. | Fixing a8b4fb2, unique installed row keys; no cost change | Testing unchanged native A journey passes; full three-engine gate follows |
| P6-T006 | Blocker / cba6d5f | Reproduction comparator accepts peak time+5e-7 s and row count+.001 by applying watts/USD tolerance to unrelated fields. | Building708432, field/quantity-aware declared tolerance dispatch | Testing preserved18pass/2fail before, then26/26 after including unchanged mismatch regressions |

The bounded runner improvement `0545341` batches up to10 physical seconds per canonical call while retaining the declared solver timestep and every dispatch observation. Whole metrics/transfer evidence and peak value/time are unchanged; only redundant chunk-start observation counts decrease. The local representative120 s run improved1746→255 ms. Independent metric-equivalence assertions remain required; this is an implementation repair, not an increased timeout.

Acceptance-harness correction: an initial browser refresh/recompute assertion compared arrays in worker completion order. Worker completion order may differ; stable run IDs already identify equivalent cells. Orchestration inspected the real asynchronous runner and approved sorting by run ID before the same full-metrics deep-equality assertion. Failed order evidence is retained. No numerical assertion, tolerance, timeout, retry or skip was weakened. Likewise the initial T004 test used unnecessary81-cell thermal work and hit its existing5s unit limit; the corrected12-cell central-plus-idle transfer fixture retains the identical incomplete-scope assertion. The separate real120s thermal sizing test remains intact.

## P6-V001 — Selected experiment binding after edits

Independent Verification reproduced this blocker in a real Chromium session against `a324447` implementation: inspect III eligible feeder, load the declared experiment with recovery dwell5 s, then edit the decision dwell to8 s. The Run selected experiment button remained enabled; its native project export still executed the old5 s definition and confirmed at9.375 s. Stable logical definition IDs were incorrectly treated as complete input identity, and generic replay reused the old definition. The expectation is explicit reload after changing the selected definition and exact planned initialization/inputs when running it. Fixing received exclusive DecisionPanel/TwinApp leases; Testing and Verification own regression/retest. Freeze is held until independently closed. Native proof: `verification/loaded-identity-browser.*` in retained evidence. No numerical thresholds are relaxed.

P6-V001 repair `c3f812a` (integrated `46e32e9`) compares complete resolved design, experiment definition and actual initial checkpoint identities before enabling a selected run. It preserves the active project, restores the exact selected initial experiment and then executes it; generic Replay is unchanged. Independent Verification closed the defect with actual before/after exports: editing5→8 disables Run until reload, reloading8 retains19 accelerator-s loss and no confirmation by12 s, and manual workload inputs no longer contaminate the selected nominal. Testing's two unchanged regressions independently verify definition binding, clean planned inputs, preserved generic Replay and byte-equal prior-project recovery. Final source-wide gates still follow this closure.

## P6-CI001 — Aggregate CI job budget

Candidate `3dcc030` source CI [34624189175](https://github.com/Arhaan2/neptune-2035/actions/runs/34624189175) passed 784 unit/integration tests and 68 browser cases, with one historical Chromium exclusion, before the job exceeded its configured 15-minute limit. Twelve planned positions lacked completed results; no browser assertion failure was logged. Native unit JSON, the job log, the explicit timeout annotation, and the tested artifact are preserved. The missing final browser JSON is disclosed. The run is incomplete and cannot authorize promotion.

Testing independently confirmed the native counts and timeout cause; Verification separately reproduced artifact parity and checked the partition. Fixing `8bb74484` (integrated `3169813`) separates core acceptance and the three browser projects while preserving all six test files, one browser worker, zero retries, the two original exclusions, and all existing timeouts. Every browser consumes the same immutable core artifact with source/tree/lock/archive/manifest/payload verification. The existing aggregate acceptance name requires core and every browser job to succeed. Independent review caught and corrected the draft artifact-download directory assumption before integration. Executable guard probes accepted the original artifact and rejected changed source, tampered bytes and extra inventory.

The repair is independently reviewed; CI001 remains pending complete native local and source CI execution on the replacement frozen candidate. Final closure belongs in the external release receipt, preserving the tested source tree.
