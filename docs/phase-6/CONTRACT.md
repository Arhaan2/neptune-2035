# Phase 6 decision contract · decision-campaign-1

Simulated, design-stage prototype; physical validation pending.

Frozen before ranking: 2026-09-11. This bounded policy answers which evaluated design is preferable under explicitly declared requirements; no global optimum or validated engineering requirement is claimed.

Minimum included cost compares the same integer requested useful workload. Maximum passing workload ranks integer requested accelerator counts, descending, with zero nominal unmet demand. The fixed reference menu is II, III transfer enabled, and the same III installed hardware with policy disabled (three candidates). The sizing menu is II at 8, 16, 24, 32, 40, 48 accelerators, full whole servers on supported catalog geometry. No binary search or skipped capacity inference. Excludes Generation I, fractional servers/platform transfers, meshed or independent sources, alternate controller physics, continuous search, and unevaluated sizes.

Named fixtures A transfer, B receiving bus/common source, C nominal only, D sizing, E sensitivities. A/B: 24 accelerators, 0.8 workload, 291.15 K sea, no UPS, required cluster/external network, 12 s cold observation, fault absolute evaluation t=2 s, transfer delay 2.375 s, 5 s recovery dwell. Nominal unmet demand=0. Fault unmet demand <=24 accelerator-s, total interruption (serviceViolationS interval union) <=3 s, thermal-limit duration=0, dwell-confirmed recovery by absolute evaluation t=10 s. Original Phase 5 zero-outage evaluation remains in raw evidence and may say FAIL while this explicit Phase 6 policy passes. Recovery onset cannot substitute for confirmation; absent confirmation after a qualifying interruption fails. Longest contiguous interruption duration is also exported. No disturbance without a resolvable asset is admitted.

Sizing uses six II candidates, nominal12 s plus cold thermal120 s, operating upstream ceiling120000 W, independently installed source rating30 MW and existing installed full-peak source/path capacity screen. Every requested accelerator must receive useful service in nominal/thermal runs. Maximum upstream draw includes every canonical dispatch interval and boundary, root/platform network draw, module cooling, conversion and standby draws already modeled. Whole-run peaks are not inferred from chart samples. Battery energy and power are zero in demo menus; portable definitions retain actual initial checkpoints. Horizon-limited outcomes never imply indefinite battery support.

The exploratory OFAT menu is central idle0.3, clean UA350000 W/K, fouling0 K/W, cost scale1; lower/upper idle0.2/0.4; UA280000/420000 W/K; fouling0/0.000002 K/W; cost0.7/1.5. The lower fouling case deliberately equals the clean central case and supplies no independent evidence. UA and fouling both affect effective exchanger conductance and are overlapping physical assumptions, not independent robustness evidence. All comparable candidates receive each case and complete corresponding suite. Sensitivity fixture adds cold120 s thermal observation. No settling is requested or asserted. This observed short thermal window establishes no long-term adequacy or joint-case robustness; bounds are assumptions, not manufacturer data or confidence intervals.

Costs come only from billOfEquipment and authoritative installed catalog/configuration. Equipment is scaled first, installation20% next, contingency25% of equipment+installation next. Central and upper bound budgets use central and1.5× central included cost respectively; cost sensitivity sets the actual equipment scale and applies the same existing order once. Upper-budget and upper-ranking modes always anchor to the original campaign central estimate times1.5, including within the cost-upper sensitivity; they do not compound to2.25. Central-budget mode uses the currently assessed sensitivity estimate. Unknown included prices never become zero or a passing estimate. Prices are assumed USD and excluded land/shore/finance/permits/tax/mooring/operation/replacement costs remain visible. Included cost is neither ownership cost nor a construction quote.

Constraint classes remain distinct: hard configuration/topology/geometry/installed peak electrical/network limits; operating whole-run service, interruption, thermal duration, confirmation deadline and supply ceiling; and unassessed marine stability/protection/site/environment/physical validation. A simulated shortage is completed-but-infeasible, not a numerical failure. Requirements export actual, unit, threshold, operator, absolute tolerance, signed threshold-minus-actual margin, scenario/sensitivity, relevant assets/time and reason. Exact full precision determines classification: violated below minus tolerance, binding within tolerance or1% of threshold, otherwise satisfied. Tolerances:1e-8 s and accelerator-s,1e-6 W/K,0.01 USD,0 accelerators. Feasible-only rank; ties within objective tolerance get shared winner/rank and stable-ID presentation order; no opaque score.

Envelope:12 candidates,5 scenarios,9 sensitivities,540 cells,12 modules/candidate,120 s observation,250000 total module steps, two workers maximum,160 trace samples/run. Full aggregate metrics and actual initial states survive trace reduction. Required scenarios share their exact matching unfaulted12 s baseline within each candidate/assumption; thermal120 s is its own nominal observation. Different failure footprints remain explicit. Execution and feasibility are separate. Unresolved/cancelled/resource-limited/numerically failed cells prohibit a final whole-search recommendation; fully evaluated candidates can provide provisional results. No persistent physical cache is used. Each restart gets fresh isolated state and an abortable bounded worker pool; late responses are discarded.

Exports include portable resolved designs/specifications, definitions, initial checkpoints, raw Phase4/5 evidence, peak observations, requirements, rankings, complete unresolved/infeasible coverage, versions and supplied source identities. Imports are structurally/version/size validated supplied evidence. Reproduction reruns the declared matrix, compares full metrics and ranking within declared tolerances, and does not trust the exported winner. Identity checks detect mismatches, not cryptographic authenticity.

Reproduction comparison uses field units, not broad object-path labels. Resolved planned definitions/initial checkpoints, discrete counts, schema versions, integration step, thresholds and declared tolerances compare exactly. Observed seconds and accelerator-seconds use1e-8; upstream watts and temperatures kelvin use1e-6; USD uses0.01; accelerator counts use0. Requirement actual/margin fields inherit their explicit unit. Remaining continuous Wh, geometry, controller fractions and rates use an absolute1e-6 fallback; runtime solverMs is excluded. A peak's time is compared as seconds, its sample count exactly, and its value as watts. Cost-row quantities are exact even though their neighboring prices use USD tolerance.
