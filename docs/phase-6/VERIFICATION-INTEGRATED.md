# Independent Phase 6 integrated engineering review

Reviewer `/root/verification` resumed the same isolated worktree and identity after the fourth active slot rotated through distinct Fixing. No production implementation or acceptance test was authored by this reviewer. Native independent probes and actual browser exports are outside source at `/private/tmp/neptune-phase6-evidence/verification/`.

**Pre-freeze engineering review: approved within the declared bounded Phase 6 model envelope.** This review initially executed source `b4650d655d05bb23b4c57625c47188ec6ed9266c`, tree `b2b688bc888de27c4cdeca637e0f154eb14d6990`, whose production code matches integration `a324447`. Later integration changes through `82f1e91` were documentation and tests only. The focused selected-run repair `c3f812aba9d75de36e636266f5bfc8590a3db01c` was then independently reviewed and retested in local merge `fd084fa304bb16c8d65b9beac07f874dbfc8cbea`, whose UI implementation matches integration `46e32e9`. The physical engine/evaluator/ranking/evidence code used for the independent campaign oracle is unchanged by that UI repair. No reviewer engineering blocker remains; exact frozen-source and release gates remain separate.

## Independent campaign enumeration

The reviewer ran all six actual declared campaign fixtures, totaling 120 candidate/scenario/sensitivity cells, exported each through the real evidence API, imported it as supplied evidence, and repeated the six-cell transfer campaign using fresh serial execution. **338 independent assertions passed.** The oracle derives service, total interruption, thermal, supply and sustained recovery decisions from raw whole-run metrics and recovery episodes. It independently rechecks the existing full-peak electrical/geometry/network screens and authoritative equipment cost, then sorts feasible candidates by the declared objective. It does not call the production Phase 6 constraint evaluator or ranking function as its oracle.

| Declared scope | Independently reproduced result |
| --- | --- |
| A eligible feeder, 6 cells | III enabled is the sole feasible winner. II and III-disabled each lose 80 accelerator-s; III loses 19. All faulted original Phase 5 outcomes remain FAIL. Included premium remains USD 240,000. |
| B receiving bus, 9 cells | No feasible candidate; the additional bus scenario prevents a false III recommendation. |
| B common source, 9 cells | No feasible candidate; there is no independent source. |
| C nominal only, 3 cells | All three pass the narrower suite; lower-cost II wins. |
| D six sizes, 12 cells | Feasible {8,16,24,32,40}; largest evaluated passing workload is 40. |
| E paired assumptions, 81 cells | III enabled remains the sole feasible winner across the nine tested OFAT cases when budget is unset. Joint combinations remain untested. |

Production sizing maxima match the prior independent per-interval grid-energy oracle within the declared 1e-6 W tolerance. Forty accelerators draw 113,505.311811 W, leaving 6,494.688189 W; 48 draw 124,361.611487 W and exceed the 120,000 W ceiling by 4,361.611487 W. Every size is explicitly executed; none is skipped by a monotonicity assumption.

For the USD 50 million budget boundary, the reviewer reevaluated the identical physical evidence under both declared economic policies and compared independently calculated results. Central budget accepts III at USD 43,170,000; its cost-upper case at USD 64,755,000 becomes infeasible. The upper-bound budget uses that USD 64,755,000 once and rejects all nine cases, including cost-lower; it does not compound upper sensitivity and upper budget to 2.25 times central. This economic-only re-evaluation is explicitly distinguished from another physical execution.

Every cost-lower/cost-upper module state and full whole-run metric history equals the corresponding central physical history. The two complete transfer executions, parallel then fresh serial after import, agree through the real reproduction comparator. Imported evidence retains its supplied-evidence label.

## Supply and thermal accounting

The observer is placed after actual interval dispatch and before the post-event display solve can overwrite power, with separate initialized and post-event boundaries. It includes root/platform network power through the canonical active electrical topology in addition to module draw. Six scenarios were executed both with and without observation: physical states and all aggregate metrics agree exactly after excluding diagnostic wall time. Observed `gridW × actual dt` integrates to the engine's separately accumulated upstream energy within 1e-8 Wh. The 4–4.375 s and 4.375–5 s transfer subintervals and the 4.375 s boundary are all present. This probe passed **21 assertions**, including three candidates whose UA and fouling cases measurably change their actual 120-second coolant states.

The installed full-peak path screen remains separate from the operating supply ceiling and does not reduce design peak for utilization. No battery energy/power is installed in these menu specimens. Cold thermal observations establish neither settling nor long-term thermal adequacy. UA and fouling act through the overlapping resistance model; the clean lower-fouling case duplicates central. The old endpoint sizing recommendation was removed from the Compare UI.

## Independently closed selected-run blocker

P6-V001 was reproduced in actual Chromium on the integrated application, with complete native project exports:

1. Load III's eligible-feeder experiment with dwell 5 s; edit the decision dwell to 8 s. The selected-run button remained enabled and the exported completed experiment still used dwell 5 s, confirming recovery at 9.375 s. Stable definition ID alone did not bind the edited planned definition.
2. Load III's nominal experiment at utilization 0.8; use Operate's recorded Full load command, then return to Compare and run the selected experiment. The export used utilization 1 and a derived recorded-input replay definition, rather than the selected nominal definition and initial state.

The scope was transferred to distinct Fixing, which implemented `c3f812a`. The reviewer inspected full design/definition/actual-initial-state binding, the exact planned-state restoration before selected execution, and preservation of the preceding project. Generic recorded-input Replay remains unchanged.

**P6-V001 independently CLOSED.** Both unchanged native Chromium reproductions were rerun after the repair. Editing dwell 5→8 now disables Run selected until explicit reload. The interactive Full load case now exports the original selected nominal definition, utilization 0.8 and no injected events. A third complete native journey explicitly reloads the eight-second dwell and runs it: its export has dwell 8 s, 19 unmet accelerator-seconds and null recovery confirmation at the 12 s observation endpoint. The required confirmation would occur at 12.375 s, outside the observed window, so this cannot masquerade as confirmed recovery. Monitored journeys show no browser errors.

## Native checks and evidence provenance

Reviewer executions passed 14 Phase 5/6 test files and **268 tests** on the initial integrated code. After the late unit-specific reproduction assertions were integrated, the reviewer reran the affected persistence file: **20 tests passed**. These native reviewer executions remain separate from Testing's complete gate and must not be added to its unique test inventory.

Native files include six `integrated-<fixture>.json` exports, `integrated-transfer-recomputed.json`, both `integrated-budget-<basis>.json` exports, `integrated-summary.json`, `integrated-probe.mjs/log`, `dispatch-observer-probe.mjs/json/log`, `integrated-tests.log`, and `comparator-final-test.log`. Both reproduced UI failures are preserved as `loaded-identity-browser-before.json`, `selected-interactive-browser-before.json` and the corresponding original logs. The unchanged `.mjs` probes, `*-after.log` and latest native exports record independent repair closure. `loaded-eight-second-browser.mjs/json/log` records explicit reload and correct incomplete dwell confirmation.

The first independent campaign oracle incorrectly assumed `pendingRecovery` is cleared after confirmation. The existing Phase 4 schema retains the current confirmed episode there. The original failure is preserved in `oracle-pending-misread.log/json`; correcting the independent oracle to inspect confirmation, onset, required dwell and terminal health produced the passing 338-assertion run. No application, production metric, threshold or tolerance was changed for that harness correction.

Exact frozen-source review, mandatory complete local gate, source CI and hosted artifact/browser acceptance remain separate requirements. Simulated, design-stage prototype; physical validation pending.
