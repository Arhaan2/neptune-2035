# Phase 6 fixing handoffs

## P6-T001 — Missing mandatory requirement values

- Role: distinct Fixing agent `/root/fixing`; repair branch `codex/neptune-phase6-fixing` in an isolated worktree. Building explicitly transferred the `src/twin/decision/contract.ts` lease before editing.
- Affected contract commit: `ee40a702754478363bb7062bebf27d6b232eefcd`. Local repair base `6c7cb0f` is its cherry-pick onto accepted Phase 5 `42a03e171f1d722874328b54fbdb2d3520ff3cc6`.
- Contract version: `decision-campaign-1` (unchanged). Required fields were already mandatory in the frozen contract.
- Reproduction: delete any of `requirements.faultUnmetAcceleratorS`, `requirements.totalInterruptionS`, `requirements.thermalViolationS`, or `requirements.recoveryConfirmationDeadlineS`; validation previously accepted the malformed campaign because it iterated present entries only.
- Repair: validate every declared numeric requirement by its explicit key, including absent values. Missing metrics/requirements cannot silently bypass the mandatory contract.
- Audit: deleted each of 74 mandatory top-level and nested Phase 6 contract fields independently. Before repair, exactly the four reported omissions passed validation; after repair, all 74 deletions were rejected. Resolved `Design` validation remains delegated to its existing authoritative validator.
- Local checks: 74-field deletion probe, typecheck, and lint passed. Testing owns committed regression tests and independent retesting/closure; this fixing handoff does not independently close the defect.
- Native evidence: `contract-key-before.log`, `contract-key-after.log`, `typecheck.log`, and `lint.log` retained under the release evidence area `fixing/`. The initial direct Node probe could not resolve extensionless TypeScript imports; its native `contract-key-node-harness-failure.log` is retained separately. Rerunning the same probe through installed Jiti resolved the demonstrated harness issue without changing assertions.
- Changed implementation file: `src/twin/decision/contract.ts`. This handoff documentation is the only other committed change.

## P6-T003 / P6-T004 — Bounded worker lifecycle and incomplete search scope

- Base runner slice: `cae0df34fff9ad8643b94f4197003deb2130f726` (local cherry-pick `ffc599b`); contract `decision-campaign-1` unchanged. Building transferred `runner.ts` and `worker-client.ts` exclusively to Fixing.
- P6-T003 reproduction: a worker that starts but never replies leaves its executor promise pending indefinitely. A fake-worker/fake-clock requirement test remained unresolved at 125 seconds. The repair bounds each invocation to 125 seconds, including the existing canonical engine's 120-second limit plus startup/reporting allowance. Timeout terminates/removes that worker and returns `resource-limited` evidence with unavailable metrics. Success/error/abort clear the timer; disposal settles active invocations; stale callbacks cannot settle later work.
- P6-T004 reproduction: complete all three central runs of the III no-benefit-bus campaign while its three cost-upper runs remain pending. The global ranking previously retained `scopeComplete: true` and `no-feasible-evaluated-candidate`. The repair marks incomplete declared scope false and gives `evaluation-incomplete` while retaining the complete central assumption's own no-feasible result separately.
- Native pre-repair results: two failures in `fixing/repair-before.json` and `.log`. Local post-repair: four focused tests pass, covering watchdog, cleanup/disposal/cancellation, stale callbacks, and real-engine incomplete sensitivity coverage. Typecheck and lint pass. An initial lint warning about a disposal array copy is retained in `repair-lint.log`; the corrected disposal drains the pool before cancellation.
- Independent handoff: Testing owns closure and additional campaign regressions. `tests/phase6-fixes.test.ts` is Fixing's isolated regression file; no shared tests were edited. P6-T002's separate builder-authored null-boundary repair was independently probed using canonical `observeBoundary`; the missing terminal measurement now yields incomplete/unresolved evidence. Its native retest is `fixing/evaluate-cae0df-boundary-retest.log`.

## Bounded runner overhead

- Base: `9d96140e5a31a5c304abc6dcde0b3abd40d36aec`. Root authorized the default-envelope performance repair while Fixing retained the runner lease.
- Replaced repeated one-second outer `advanceWithStep` calls with the existing `CONTRACT.maxChunkS` (10 seconds) bound, truncating the last chunk to the declared horizon. Work accounting multiplies the actual chunk duration. Engine integration steps, transfer deadline subdivisions, every inner interval/boundary observer, scenario policies, and wall-clock ceilings are unchanged. Cancellation yields between these bounded chunks.
- Representative III central cold thermal observation, 120 seconds: 1746.251 ms before versus 254.868 ms after in this local probe (6.85×; indicative timing, not a browser performance guarantee). Full authoritative metrics and transfer state were identical, as were supply peak 194232.84517736774 W and its time 0. Supply-observation count changed from 361 to 253 because fewer outer calls repeat their starting boundary; no physical interval or terminal boundary was omitted.
- Six focused tests pass, including full metric/transfer-state/supply-peak comparisons against one-second outer calls at 1-second and 0.125-second integration. Typecheck and lint pass. Native before/after output is retained in `fixing/performance-{before,after}.{json,log}` and acceptance in `fixing/chunk-final.json`. Testing independently checks complete serial/parallel campaigns.

## P6-T005 — Duplicate installed-transfer bill row keys

- Base: integration `03dc99080f2ee13457d64bf53ebd630da027803f`, including root UI `14f9fb9` and the bounded runner repair. Successor isolated branch `codex/neptune-phase6-fixing-ui` uses the same fixing worktree. Root exclusively transferred `src/ui/TwinApp.tsx` for this repair.
- Testing's real-browser Phase 6 candidate-load journey emitted six React console errors: two each for repeated receiving-bus, original-path isolator, and normally-open transfer-tie labels. Native failure remains in `testing/browser-phase6-first.log` and the corresponding Vite log.
- The bill contains distinct installed assets with equal display labels. Its display-only ordered rows now use position plus scope for unique React identity; economic rows, prices, counts, and grouping are unchanged.
- Local typecheck and lint pass (`fixing/ui-T005-{typecheck,lint}.log`). Testing independently reruns the unchanged browser assertion. No claim of independent closure is made by Fixing.

## P6-V001 — Exact selected experiment binding and isolated rerun

- Base: `82f1e91e77851a8589023e038c4d36344a272b3d`, branch `codex/neptune-phase6-fixing-binding`; root exclusively transferred `DecisionPanel.tsx` and `TwinApp.tsx`. Contract version remains `decision-campaign-1`.
- Verification's actual Chromium reproduction loaded III eligible-feeder at a five-second dwell, edited the decision dwell to eight seconds, and ran a still-enabled button. The native export retained the old five-second definition and confirmation at 9.375 seconds. Native pre-repair proof remains in `verification/loaded-identity-browser.{mjs,json,log}`.
- A second actual Chromium reproduction loaded III nominal, recorded a shore-fiber trip at time zero, then clicked Run selected experiment. The exported run silently derived an interactive-history definition and accumulated 288 accelerator-seconds of nominal shortfall. Native before proof and unchanged executable remain in `fixing/selected-interactive-{before.json,before.log,browser.mjs}`. Verification independently reproduced this branch with an interactive workload input; Testing reproduced it with a seawater input.
- Loaded status now compares the complete resolved design, experiment definition, and original physical checkpoint identities. Changed recovery semantics require another explicit Load selected candidate action even if human-readable IDs are unchanged. Run selected experiment saves the current project in Compare, restores the selected plan's exact initial state and definition, then starts that declared run. Generic Replay keeps its existing interactive-history behavior.
- Local native Chromium retests: editing five to eight seconds disables Run selected until reload; the reloaded export uses eight seconds and has no dwell confirmation within the 12-second window. The unchanged interactive probe now exports the exact nominal definition/initial checkpoint, no interactive inputs, and zero shortfall. Both journeys report no console/page errors. Typecheck, lint, and diff whitespace checks pass.
- Native retests: `fixing/loaded-reload-after.{json,log}`, `fixing/selected-interactive-after.{json,log}`, and `fixing/binding-{typecheck,lint}.log`. Initial local server bind denial is retained as `fixing/binding-vite.log`; the isolated server subsequently started with authorized local binding. Independent browser regressions and closure remain owned by Testing and Verification.

## P6-CI001 — Partition the unchanged acceptance suite by browser

- Base: frozen candidate `3dcc030224a037171b6270d75dea35a40c932bed`, successor branch `codex/neptune-phase6-fixing-ci`. Root exclusively transferred `.github/workflows/phase-1.yml`; application code, tests, Playwright configuration, and deployment were not edited.
- Native failure: [source CI run 34624189175](https://github.com/Arhaan2/neptune-2035/actions/runs/34624189175) reached the existing 15-minute aggregate job limit. Unit/telemetry completed with 784 passes. Browsers had 68 passes, one historical exclusion, and zero assertion failures before cancellation interrupted the remaining 12 planned positions. This is incomplete acceptance, not a passing CI result. Native job/status logs and its successfully retained artifact remain under `ci-attempt-1/` in release evidence.
- Timing rationale: the job began at 16:49:26 UTC, completed core/build packaging at 16:53:36, then ran browsers sequentially until cancellation at 17:04:40. The repair gives core and each of the three browser projects separate, unchanged 15-minute job envelopes. Every browser still runs the same six supported files with one worker, zero retries, and the unchanged 60-second test / 12-second expectation limits. No acceptance case was removed or added to exclusions.
- Core performs fresh `npm ci`, typecheck, lint, all unit tests with both real Chromium telemetry integrations enabled, and one build/package/archive. It publishes a success-only immutable `tested-build-${github.sha}-${github.run_attempt}` artifact. Its exact artifact ID, tar SHA-256, source SHA and source tree feed all browser jobs. Each browser installs its own dependencies/browser, downloads that single artifact by ID, validates checkout/source/tree/lockfile, tar digest, release/manifest identity, every payload byte and exact inventory, and serves the extracted build without rebuilding.
- Native logs/reports/traces upload even on failure under `prototype-core-${github.sha}-${github.run_attempt}` and `prototype-browser-${browser}-${github.sha}-${github.run_attempt}`. The existing required check name, `Decision campaign and whole-experiment acceptance`, now aggregates core and the fail-fast-disabled three-browser matrix; failure, cancellation or skipped jobs cannot produce success. Root release tooling should obtain the promotion tar from the new `tested-build-…` artifact.
- Local checks: YAML parse and every shell block's syntax; unchanged exact six test paths and Playwright config; full archive-verification shell executed against retained CI tar `9b86e3b4e8713cad826c262ca0b5141cefe221421a0bdc838e0f02e23365c3c4`; valid archive accepted, wrong archive/source, altered payload and unlisted payload rejected. An exhaustive 16-case aggregate-status probe accepts only success/success. Native output is `fixing/ci001-validation.log` and `fixing/ci001-guard/summary.json`.
- Testing and Verification independently reviewed the partition and caught an artifact download path issue before commit: v4 requires `merge-multiple: true` to flatten an ID-based download. It is included in the final workflow. Local checksum option/implicit-stdin failures were retained; portable `sha256sum -c -` preserves the same check and allows executing the exact shell locally. The aggregate uses an explicit conjunction rather than relying on the runner's implicit shell fail-fast mode.
- Independent review supports the harness correction. Only a successful new native source CI run can close P6-CI001; these static/local probes do not claim GitHub job execution passed.
