# Phase 6 fixing handoffs

## P6-T001 — Missing mandatory requirement values

- Role: distinct Fixing agent `/root/fixing`; repair branch `codex/neptune-phase6-fixing` in an isolated worktree. Building explicitly transferred the `src/twin/decision/contract.ts` lease before editing.
- Affected contract commit: `ee40a702754478363bb7062bebf27d6b232eefcd`. Local repair base `6c7cb0f` is its cherry-pick onto accepted Phase 5 `42a03e171f1d722874328b54fbdb2d3520ff3cc6`.
- Contract version: `decision-campaign-1` (unchanged). Required fields were already mandatory in the frozen contract.
- Reproduction: delete any of `requirements.faultUnmetAcceleratorS`, `requirements.totalInterruptionS`, `requirements.thermalViolationS`, or `requirements.recoveryConfirmationDeadlineS`; validation previously accepted the malformed campaign because it iterated present entries only.
- Repair: validate every declared numeric requirement by its explicit key, including absent values. Missing metrics/requirements cannot silently bypass the mandatory contract.
- Audit: deleted each of 74 mandatory top-level and nested Phase 6 contract fields independently. Before repair, exactly the four reported omissions passed validation; after repair, all 74 deletions were rejected. Resolved `Design` validation remains delegated to its existing authoritative validator.
- Local checks: 74-field deletion probe, typecheck, and lint passed. Testing owns committed regression tests and independent retesting/closure; this fixing handoff does not independently close the defect.
- Native evidence: `contract-key-before.log`, `contract-key-after.log`, `typecheck.log`, and `lint.log` retained under the release evidence area `fixing/`. The initial direct Node probe could not resolve extensionless TypeScript imports; its native `contract-key-node-harness-failure.log` is retained separately. Rerunning the same probe through installed Jiti resolved the demonstrated harness issue without changing assertions.
- Changed implementation file: `src/twin/decision/contract.ts`. This handoff documentation is the only other committed change.

## P6-T003 / P6-T004 — Bounded worker lifecycle and incomplete search scope

- Base runner slice: `cae0df34fff9ad8643b94f4197003deb2130f726` (local cherry-pick `ffc599b`); contract `decision-campaign-1` unchanged. Building transferred `runner.ts` and `worker-client.ts` exclusively to Fixing.
- P6-T003 reproduction: a worker that starts but never replies leaves its executor promise pending indefinitely. A fake-worker/fake-clock requirement test remained unresolved at 125 seconds. The repair bounds each invocation to 125 seconds, including the existing canonical engine's 120-second limit plus startup/reporting allowance. Timeout terminates/removes that worker and returns `resource-limited` evidence with unavailable metrics. Success/error/abort clear the timer; disposal settles active invocations; stale callbacks cannot settle later work.
- P6-T004 reproduction: complete all three central runs of the III no-benefit-bus campaign while its three cost-upper runs remain pending. The global ranking previously retained `scopeComplete: true` and `no-feasible-evaluated-candidate`. The repair marks incomplete declared scope false and gives `evaluation-incomplete` while retaining the complete central assumption's own no-feasible result separately.
- Native pre-repair results: two failures in `fixing/repair-before.json` and `.log`. Local post-repair: four focused tests pass, covering watchdog, cleanup/disposal/cancellation, stale callbacks, and real-engine incomplete sensitivity coverage. Typecheck and lint pass. An initial lint warning about a disposal array copy is retained in `repair-lint.log`; the corrected disposal drains the pool before cancellation.
- Independent handoff: Testing owns closure and additional campaign regressions. `tests/phase6-fixes.test.ts` is Fixing's isolated regression file; no shared tests were edited. P6-T002's separate builder-authored null-boundary repair was independently probed using canonical `observeBoundary`; the missing terminal measurement now yields incomplete/unresolved evidence. Its native retest is `fixing/evaluate-cae0df-boundary-retest.log`.

## Bounded runner overhead

- Base: `9d96140e5a31a5c304abc6dcde0b3abd40d36aec`. Root authorized the default-envelope performance repair while Fixing retained the runner lease.
- Replaced repeated one-second outer `advanceWithStep` calls with the existing `CONTRACT.maxChunkS` (10 seconds) bound, truncating the last chunk to the declared horizon. Work accounting multiplies the actual chunk duration. Engine integration steps, transfer deadline subdivisions, every inner interval/boundary observer, scenario policies, and wall-clock ceilings are unchanged. Cancellation yields between these bounded chunks.
- Representative III central cold thermal observation, 120 seconds: 1746.251 ms before versus 254.868 ms after in this local probe (6.85×; indicative timing, not a browser performance guarantee). Full authoritative metrics and transfer state were identical, as were supply peak 194232.84517736774 W and its time 0. Supply-observation count changed from 361 to 253 because fewer outer calls repeat their starting boundary; no physical interval or terminal boundary was omitted.
- Six focused tests pass, including full metric/transfer-state/supply-peak comparisons against one-second outer calls at 1-second and 0.125-second integration. Typecheck and lint pass. Native before/after output is retained in `fixing/performance-{before,after}.{json,log}` and acceptance in `fixing/chunk-final.json`. Testing independently checks complete serial/parallel campaigns.
