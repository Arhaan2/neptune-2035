# Build log

## 2026-09-08 · Setup checkpoint
- Read complete user brief. Empty parent workspace; dedicated `neptune` project created.
- Installed official Sites scaffold with npm; adapted entry to static Vite to follow no-backend requirement. Retained starter components/dependencies.
- Verified React 19.2.6 / Fiber 9.7.0 compatibility from npm peer dependencies. Selected Three 0.186.0 and Drei 10.7.8.
- Registered dedicated unpublished Sites project. No purchases or billing changes requested.
- GitHub CLI reports invalid authentication for Arhaan2; GitHub publishing blocked pending valid credentials.
- Primary-source research delegated to one bounded worker; implementation/integration stay with lead.
- Added pure model, validation, schema-v1 sharing, canonical layout and exploded offsets.

## Correctness and visual checkpoints
- Pure model and scenario suite: 53 tests passed. Type-check and production build passed.
- Inspected real Chromium hero and X-ray screenshots. Corrected narrow framing, unreadable small labels, missing mobile icon names, and foreground placement of representative flow traces.
- Initial browser suite exposed canvas resizing on inspector changes and a missing malformed-hash notice on same-document navigation. Fixed both; targeted Chromium regressions passed.
- Screenshot inspection exposed the guided camera being constrained by the exterior zoom minimum. Fixed interior orbit limits; inspected an actual rack aisle after the fix.
- Corrected the glyph disclosure: geometry illustrates capacity, including partial modules and platform aggregation. Exact inventory remains model-derived.
- Rechecked GitHub authentication outside network sandbox: authenticated as Arhaan2. Initial invalid-token report was a sandbox artifact, not a missing credential.
- Hosting choice: GitHub Pages for a new public repository. Official GitHub documentation explicitly includes public Pages on GitHub Free. Sites exposes no pricing/plan metadata in its connector, so the reserved Sites project remains private and unpublished rather than assuming a free hosting entitlement.

## Resume and release hardening
- Resumed after a user interruption. The interrupted preview server had stopped; a subsequent browser run failed only with connection-refused errors. Added automatic local-server startup/reuse to Playwright and restarted the preview.
- Investigated all 11 starter dependency advisories. Removed unused Vinext/RSC/Workers tooling and dead server entry files because the delivered app is static. Updated Vite to 8.2.2 and test-image tooling to Sharp 0.35.4; applied the compatible Undici update. npm reported zero vulnerabilities afterward.
- The system Swift/Xcode loader could not initialize, so native AVFoundation compilation was unavailable. A temporary npm FFmpeg binary provides local H.264 transcoding instead; no encoder binary is shipped.
- All 15 local browser cases passed across Chromium, Firefox and WebKit. A keyboard assertion was corrected to await the next rendered frame instead of reading stale telemetry.
- Captured the real storyboard to WebM and inspected the actual social preview and portrait screenshot. The latter exposed footer overlap with the toolbar; presentation now allocates canvas space from the footer's actual height and has an explicit bounds regression check.
