# Campus Step 10s repair and verification

The historical 627a29d push failed the Firefox transition journey's 12-second `data-time=10` assertion; its PR integration run passed. The old artifact did not contain actual traces, so that exact failure cannot be assigned a proven internal cause.

The c5d7be3 diagnostic candidate retains actual Playwright files separately from source evidence, even when report parsing fails. Hosted Ubuntu 24.04 / x86_64 / Node 24 / locked headed Firefox with the existing Xvfb/software graphics configuration ran an intentional harness failure. Its separate artifact contains a readable trace ZIP, a 17,199-byte screenshot, raw JSON and the bounded attachment. The transient forced-failure test was removed, and the harness generator is removed from the repaired candidate. This is retention validation, not acceptance.

The predeclared initial batch stopped after its first actionable fresh 391-module family-III campus observation: Step 10s completed in 14,822 ms with time exactly 10 and controls enabled. The current design and initialized revision matched (`reference-v2-0ccf7369`); request 13 / epoch 4 dispatched and was accepted once. Worker receipt to progress was 8,437 ms, including a single 10-second chunk taking 4,467 ms; worker completion followed at 8,854 ms. The UI validated, constructed a project, summarized and persisted the same terminal boundary twice, plus an additional terminal persistence. The three saves took 368, 363 and 354 ms, with additional duplicate validation/project/summary and scene rendering. The operation completed; neither a deadlock nor a lost request was observed. This establishes avoidable work on a slow correct operation, not a retrospective trace of the original failed run.

The repair sends intermediate progress only while a target remains, then publishes the validated terminal checkpoint once. The UI's existing `accept()` remains the single terminal persistence path and new requests clear previous progress. Validation, checkpoint admission, atomic durable-write reporting, numerical integration, event boundaries, campus size, graphics settings, ordinary assertion deadlines and retries are unchanged. The worker regression fails on the prior code's duplicate terminal publication. A real-worker browser regression holds responses, checks that the clock stays unchanged, rejects stale progress display, verifies one current-design advance, then verifies exact completion, one persistence and an export without a pending target. Existing cadence tests now use three chunks so they still test both sides of the 250-ms intermediate-progress threshold.

The repaired candidate's hosted stability plan is fixed at 20 executions, alternating ten fresh-campus steps with ten copies of the complete original camera/render/design transition journey. There are no retries, shortened scenarios or diagnostic sleeps in that stability gate; all outcomes are retained, and any failure rejects the batch. Diagnostic instrumentation is explicitly enabled for the batch only and bounded to scalar request identities, revisions and timings. Normal full acceptance does not enable instrumentation.

Run/evidence: [diagnostic push 34527903080](https://github.com/Arhaan2/neptune-2035/actions/runs/34527903080), separate `phase-1-campus-observations-c5d7be33bfffe0b4af1c2b91bd6a229fc3574e96` artifact. Downloaded evidence is preserved locally under `artifacts/phase-1/campus-repair/diagnostic-run/`. Fresh candidate stability, full push/PR/main acceptance and production deployment must be recorded in the external release receipt before closure; this document does not claim those gates have passed.

## Measured campus-only deadline

The first repaired 20-execution batch at `348866f` ([push 34528865633](https://github.com/Arhaan2/neptune-2035/actions/runs/34528865633)) completed **19/20 tests**, with ten original transition journeys passing and one fresh-campus case failing its 12-second `data-time` assertion. All twenty operations ultimately reached exactly 10s with ready controls. Execution 13's actual trace ZIP and worker/DOM records show a single request for the matching design, worker completion at 10,894 ms after receipt, one UI checkpoint construction/persistence, and the DOM committing 10s/ready approximately 13,769 ms after dispatch. Its retained final observation was 14,015 ms. The first terminal-work repair reduced duplicate processing, but did not establish a reliable 12-second completion bound. No lost request, retry, checkpoint error or unbounded busy state was observed. These results do not retroactively prove the precise internal timing of the untraced 627a29d failure.

For the two 391-module campus cases only, completion now has a shared **20,000 ms ceiling measured from the click attempt through exact numerical completion and ready controls**. This covers the observed 14.0-second maximum with explicit margin; it is far below the general 120-second worker ceiling. A separate **3,000 ms acknowledgement ceiling** requires either unchanged simulation time with Step disabled and Cancel available, or an already completed exact step. The ordinary 12-second assertions elsewhere and 60-second whole-test limit remain. The next fixed batch again runs all twenty executions without retries and rejects any failure. Raw latency and acknowledgement durations are attached for every case.

The separate diagnostic artifact now includes hidden files as well, retaining Playwright's `.last-run.json` alongside the already verified raw report, actual ZIPs, screenshots and attachments. No source-protection baseline was changed.
