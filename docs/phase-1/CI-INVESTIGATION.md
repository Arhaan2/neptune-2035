# Hosted CI investigation

Local implementation acceptance remains PASS at `76cdbcc7c62762c3464b289f97e75bfb6baa6850`; the evidence-only published head is `82b2a6fbd796fd71968596dcc1157ea02d55be51`. Integration is NOT MERGED while the hosted gate is unresolved.

The first source-push run [34391506009](https://github.com/Arhaan2/neptune-2035/actions/runs/34391506009) completed with failure. Clean installation, typecheck, lint, build and all 256 unit cases passed. Browser results were 42 passed / 33 failed / zero skipped / zero flaky: Chromium 25/25, Firefox 17/25, WebKit 0/25. The browser stage took 2,461.27 seconds. Measurements were not executed after the failed browser gate. [Raw execution](evidence/ci-macos14-push/execution.json), [browser errors](evidence/ci-macos14-push/browser.log) and [case summary](evidence/ci-macos14-push/browser-summary.json) preserve the actual failures.

All 25 WebKit failures occurred while creating a page, before application assertions: `Protocol error (Page.overrideSetting): Unknown setting: PushAPIEnabled`. The installer explicitly warned that mac14-arm64 receives a frozen WebKit build (revision 2251). The locked Playwright package uses current WebKit revision 2359 elsewhere; the local macOS 26.6 run used WebKit 26.6 successfully. This is concrete browser/driver incompatibility evidence, not an application test rejection.

The eight Firefox failures were interaction/render-readiness timeouts in the existing visual/clock suites. All nine Phase 1 Firefox browser cases passed. Those failures are retained as unresolved hosted-environment observations until the corrected runner passes the complete suite; their precise resource/OS cause is not proven. The first runner reported virtual Apple M1, three logical CPUs, 7 GiB RAM, macOS 14 (Darwin 23.6), Node 24.20.0 and npm 11.19.0. A local `CI=true` comparison with both telemetry flags passed 256/256 unit cases in about seven seconds; it does not substitute for hosted acceptance.

The PR-specific run [34391548016](https://github.com/Arhaan2/neptune-2035/actions/runs/34391548016) reached its configured 45-minute job deadline and was cancelled by GitHub. It was not cancelled manually and is not counted as a pass. The check annotation states “The job has exceeded the maximum execution time of 45m0s”. Before cleanup, the verification script completed with FAIL and uploaded its evidence: 256/256 unit cases passed; browser results were 41 passed / 34 failed / zero skipped / zero flaky (Chromium 25/25, Firefox 16/25, WebKit 0/25). Measurements were not executed. [PR execution](evidence/ci-macos14-pr/execution.json) and [PR browser summary](evidence/ci-macos14-pr/browser-summary.json) distinguish the failed gate from the cancelled overall job.

## Scoped correction

The workflow runner changes from `macos-14` to `macos-26-intel`. GitHub documents this as a standard public-repository runner with four CPUs and 14 GB RAM; it also avoids the obsolete macOS-14 WebKit override. See [GitHub runner specifications](https://docs.github.com/en/actions/reference/runners/github-hosted-runners) and [Playwright CI guidance](https://playwright.dev/docs/ci). The OS/browser correction is based on the explicit WebKit protocol failure. Additional memory/CPU headroom is intended to test the Firefox timing hypothesis; a successful complete run is still required.

All application code, numerical contracts, integration settings, test assertions, test counts, timeouts, telemetry flags, dependency lockfile and browser launch configuration remain unchanged. The same complete verification entry point runs on source push and PR, including all 75 browser cases and measurements. There is no skip, retry-based green result, deployment/release step, permission increase or branch-protection bypass. The existing source-only Pages separation remains intact.

The corrected head must pass normal CI and have its prospective merge re-reviewed against current main before merge. Actual corrected CI and post-merge results are recorded in the PR/final integration record, preserving these failed attempts.

## Current Intel runner: bound test-file concurrency

The next head, `264de666fac69c575e17186ec1b7342fcf6fd28e`, ran on macOS 26 / Darwin 25.6 x64, four virtual Intel i7-8700B cores, 14 GiB RAM, Node 24.19.0 and npm 11.17.0. Browser installation, clean dependency installation, typecheck, lint and build passed. The [push run 34397051589](https://github.com/Arhaan2/neptune-2035/actions/runs/34397051589) passed 254/256 unit cases; the [PR run 34397058602](https://github.com/Arhaan2/neptune-2035/actions/runs/34397058602) passed 255/256. Both failed; neither browser acceptance nor measurements was executed. [Push evidence](evidence/ci-macos26-intel-push/execution.json) and [PR evidence](evidence/ci-macos26-intel-pr/execution.json) preserve the results.

In both runs, the existing million-accelerator inventory test exceeded its unchanged default 5-second test deadline, completing after 10.86 / 10.61 seconds. The push run also exceeded the telemetry app case’s unchanged 40-second deadline; that same case passed in 30.28 seconds on the PR run. The evidence reports timeouts, not incorrect numerical assertions. Other allocation-heavy persistence files and actual browser telemetry were running concurrently. Resource contention is a hypothesis supported by the overlapping workload, not a proven diagnosis for every earlier Firefox failure.

The scoped next correction sets `test.fileParallelism: false` in `vite.config.ts`, so Vitest schedules one test file at a time in local and hosted runs. The [Vitest contract](https://vitest.dev/config/fileparallelism) specifies that this uses one worker without changing within-file tests. Default isolation, all 256 cases, the tests’ real worker/browser concurrency, assertions, numerical tolerances, timeouts, telemetry flags, and full browser/measurement gates remain unchanged. Runtime code and the dependency lockfile are unchanged. This reduces simultaneous test allocation/CPU pressure without claiming that hosted acceptance has passed. The affected full unit suite is rerun locally; the exact next committed head must pass the complete normal hosted gate.

The affected local `CI=true` unit rerun passed all 256 cases with both telemetry flags and zero skips in 17.13 seconds, with the same test deadlines. [Local scheduling probe](evidence/ci-serial-local/summary.json) records the command, runtime and changed configuration hash; it is explicitly not a clean hosted-acceptance substitute.

## Retained Intel serial failures and current Apple Silicon runner

The serial head `21404e192d13e41f60a7f48488c29592d35b1b81` still failed on the Intel runner: [push 34398209413](https://github.com/Arhaan2/neptune-2035/actions/runs/34398209413) passed 254/256 and [PR 34398216144](https://github.com/Arhaan2/neptune-2035/actions/runs/34398216144) passed 255/256, with zero skips. The campus case exceeded its default 5-second deadline at 11.43 / 5.84 seconds; the push telemetry case exceeded 40 seconds. Browser acceptance and measurements were not executed. [Serial push evidence](evidence/ci-macos26-intel-serial-push/execution.json) and [serial PR evidence](evidence/ci-macos26-intel-serial-pr/execution.json) preserve both failures. Sequential files did not establish the proposed contention explanation or produce hosted acceptance; it remains useful for bounded allocation.

The next one-line runner change uses standard `macos-26` (three M1 CPUs, 7 GiB RAM), as documented in the GitHub runner table linked above. This combines the Apple Silicon architecture on which both original macOS-14 runs passed every unit test with a current OS/browser build that avoids the frozen WebKit override. All assertions, timeouts, telemetry flags, browser launch settings, default isolation, numerical runtime code, dependency lock and source-only CI steps remain unchanged. The smaller memory envelope is explicitly recorded; no inference of universal performance or a successful Firefox correction is made. The complete next push and PR gates, including browser acceptance and measurements, must actually pass before merge.

## Current Apple Silicon results and browser execution corrections

At `77592c4f18fc85d09d2b6ac96cad48ed8477b770`, the [push run 34399388324](https://github.com/Arhaan2/neptune-2035/actions/runs/34399388324) and [PR run 34399395431](https://github.com/Arhaan2/neptune-2035/actions/runs/34399395431) passed installation, typecheck, lint, build and all 256 unit cases. Browser results were 64/75 (push) and 67/75 (PR), with zero skipped/flaky cases; measurements were not reached. The PR passed all 25 Chromium and 25 WebKit cases; eight Firefox cases failed. Push passed all 25 Chromium, 24 WebKit and 15 Firefox cases. [Push evidence](evidence/ci-macos26-arm-push/execution.json) and [PR evidence](evidence/ci-macos26-arm-pr/execution.json) retain both failed gates.

The original WebKit protocol incompatibility is resolved on the current OS. The single push-only WebKit failure occurred in the cancellation test’s final console-error assertion: repeated ANGLE/Metal `OUT_OF_MEMORY` messages reported failed host-memory allocation. Its numerical cancellation assertions had passed, and the same full WebKit suite passed in the PR run. This is an observed renderer allocation failure on a 7-GiB VM, not evidence of a corrupt numerical checkpoint.

Firefox still showed frame/input stalls and post-Pause clock drift (60 to 200 seconds on push, 100 to 260 on PR). Old failure summaries lack the final control-state snapshots, so they do not establish whether the click was suppressed or a stopped clock resumed incorrectly. The next verification script retains sanitized textual error contexts for future failed cases; screenshots, traces and recordings are not added to Git. A separate regression using delayed responses from a real worker investigates the busy Pause control and stale-response path.

The next browser configuration runs Firefox with a visible window (`headless:false`) and sets the WebKit desktop project to `deviceScaleFactor:1`, matching Chromium/Firefox’s standard pixel density at the unchanged 1600-by-1050 CSS viewport. The [Playwright CLI](https://playwright.dev/docs/test-cli) distinguishes headed execution from debug mode, which disables deadlines; debug mode is not used. These settings preserve all functional assertions, browser coverage, scene geometry, viewport dimensions, telemetry and timeouts. Standard density reduces the backing pixel count relative to the previous Retina fixture; results must be interpreted with that explicit display configuration. This is an execution-environment correction, not a change to product rendering or a claim that all hardware configurations are equally fast. Full local and hosted acceptance is required for the final candidate.

During the local focused run, Vite reported watching generated trace HTML under `artifacts/` as a page-reload source. The retained CI server logs do not show the same message, so this is not asserted as the hosted failure cause. The dev watcher now excludes generated `artifacts/`, `test-results/`, and `assets/screenshots/` output, keeping verification output separate from source watching. Source modules, runtime public assets, test assertions and polling of actual source remain intact.

## Independently reproduced Pause defect

A new real-browser regression held actual responses from a real simulation worker after the worker completed an advance, while the UI still showed its earlier validated checkpoint. Before the repair, Pause remained disabled throughout the pending request and failed the desired enabled-control assertion (28 disabled-button observations within the existing 12-second assertion deadline). The repair keeps Pause enabled during clock advances, synchronously clears and guards the interval, invalidates the request generation and cancels outstanding work. Delayed advance and cancellation replies cannot change the chosen committed checkpoint. Start remains disabled during conflicting manual Step work. The same test verifies export consistency and successful continuation afterward.

All 33 focused browser cases passed: the now-ten Phase 1 cases in each of three browsers plus the existing clock case in each browser. The complete suite therefore contains 256 unit cases and 78 browser cases; all original 75 browser cases remain. Typecheck and lint passed. This establishes the specific busy-Pause defect and its repair; it does not retroactively prove that this caused every older Firefox failure. Full clean acceptance and independent review of the final candidate still govern integration.
