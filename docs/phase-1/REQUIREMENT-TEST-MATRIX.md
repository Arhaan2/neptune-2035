# Phase 1 requirement–test matrix

All current regression files are included by the normal `npm test` / `npm run test:browser` scripts. Full acceptance uses `node scripts/phase-1/verify.mjs`; it installs dependencies cleanly, runs typecheck/lint/build, enables **both** documented telemetry flags, runs all three browsers without skips/retries, verifies immutable prerequisites, and measures real engine histories. `.github/workflows/phase-1.yml` invokes this same gate on source pushes and PRs to `main`.

| ID | Implementation | Durable test / command | Evidence |
| --- | --- | --- | --- |
| PH1-NUM-01 | `solvers/hydraulic.ts`, `safety.ts` | `tests/phase1-numerical.test.ts`: explicit pump optional-rating rejection; historical `audit.mjs PH0-001` and `--desired` at the pinned checkpoint | `evidence/history/investigation.json`, successful historical attempt PH0-001 JSON; current full unit summary |
| PH1-NUM-02 | Four solvers; `persistence/design.ts`, `events.ts`, `state.ts`; geometry and report boundaries | `phase1-numerical`, `phase1-persistence`, `phase1-worker`: omission vs supplied invalid values, units, references, zero/disabled/signed heat, unsupported vs solved inadequate, sparse allocation and worker restore | Numerical audit; current full unit summary; independent review |
| PH1-NUM-03 | Solver intermediate/output/convergence guards; atomic `simulation.ts` candidate; worker/UI commit validation | Real hydraulic nonconvergence/overflow, extreme finite loop started by actual event, unchanged input, invalid worker timer, corrupted checkpoints | Current numerical/worker unit cases and review-persistence evidence |
| PH1-PER-01 | Shared `limits.ts`, `project.ts`, `events.ts`; report import/export delegates | Real applied 999/1,000/1,001 events; real 86,399/86,400/86,401 s engine run; normalized import/export and subsequent continuation | Historical PH0-002 semantic failures; current `phase1-persistence`; `measurements.json` |
| PH1-PER-02 | Shared project/worker limits; structural preflight and exact UTF-8 counting | Below/at/above horizon, event count, UTF-8 bytes with multibyte text, nesting/items/strings/arrays, step settings, advance/chunk/wall/work budgets. `phase1-work-budget` tests actual scheduling limits with a controlled numerical kernel; other histories and trajectories use the real engine | Contract, unit summaries, bounded measurements. Allocation-shape tests do not claim every maximum combination is semantically valid or economical |
| PH1-CHK-01 | Complete schema-3 state/design identity, project checkpoint binding, fault/controller/timer/cursor checks | Battery discharge + standby startup, before/at/after events, multiple same-time events, duty restore deadline, missing/mismatched state, duplicate submissions, full graph restoration | `phase1-persistence`; `phase1-worker`; persistence adversarial review |
| PH1-REP-01 | Fixed persisted integration grid; event admission sequence; controller boundary algorithm; worker chunking | Intermediate continuous/chunked/restored/fresh-worker trajectories; exact normalized equality; all four step settings; real browser fractional-grid restore/seek | Unit summaries; `browser/phase1.spec.ts`; measurements; contract tolerance table |
| PH1-REP-02 | Request/epoch identity, yielding event validation/chunks, partial-state progress/cancel, fresh worker after interruption | Cancel/resume, event-admission cancellation, stale replies after supersession, work/wall exhaustion, real browser captured stale message during conflicting import and worker-error recovery | `phase1-worker`, `phase1-work-budget`, new browser suite |
| PH1-COMP-01 | Explicit compatibility modes; original identities retained; derived scenario parent provenance | Legacy long history, mixed legacy sequence metadata, unknown numeric/structural versions, explicit recalculation, original re-export, saved comparison parent retention | `phase1-persistence`; browser compatibility and comparison case |
| PH1-UI-01 | `storage.ts`, `useTwin.ts`, minimal `TwinApp.tsx` controls | Real save/export/load, refresh recovery, malformed import, quota error preserving old durable data, manual export, unreadable checkpoint protected, raw saved-shelf numeric overflow rejected | Three-browser Phase 1 suite; focused summaries and full gate |
| PH1-REG-01 | Existing assumptions and deferred statuses retained | Original 162 tests with telemetry; original 48 browser cases (cancel and horizon assertions updated for complete checkpoints and the retained 30-day contract); `phase1-deferred.test.ts` for PH0-003–006; isolated historical full verification and 53-file recovery | Historical attempt records, current full summaries, immutable hash verification |
| PH1-MERGE-01 | Source-only CI and history-preserving PR integration | Remote trigger/hook/Pages audit; reviewed candidate; required CI; actual remote merge confirmation; clean `--ref` post-merge verification | `MERGE-AND-ROLLBACK.md`, `evidence/remote-preflight.json`, review records, PR/CI/post-merge record when completed |

Historical desired-behavior failures are executed only in the isolated Phase 0 checkpoint. Current tests require fixed behavior; no Phase 0 file, fixture or checksum is changed. The first historical browser attempt's two WebKit timeouts are preserved, as are the unchanged targeted retry and successful full 162/48 rerun. These are reported as observed attempts, not silently omitted failures.
