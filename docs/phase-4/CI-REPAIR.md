# Phase 4 CI signature execution investigation

The first [Phase4 source CI](https://github.com/Arhaan2/neptune-2035/actions/runs/34568246585) tested PR merge `f52078a82bfecdea5d29cc0e871c065d8e3cfaf3`, parents accepted Phase3 main and feature `377ce40c54479143da52146ef6283ddd3c1f3be8`, tree `f743c89ad6659b992e6ecbce1e453fb3ac6b6885`. Its archived source identity matches the independently reviewed and locally tested tree. Install, typecheck, lint, build and all498 unit/integration tests passed, including both real telemetry integrations.

The browser gate passed34 journeys with2 historical exclusions and failed3: the genuine pair/signature journey in Chromium, Firefox and WebKit. The 12-second assertion waited for a No standby pump report that had not appeared. Native snapshots show both execution buttons disabled, the signature description rendered, and no result cards/error notice. Native traces confirm the click executed. This is not evidence of an incorrect shortfall value; it is a pending execution/readiness failure. No merge or publication occurred.

Fixing owns the production investigation, Testing independently interprets the native traces and supplies regression coverage, and Verification reviews the diagnosis and proposed change. The numerical signature remains exact in the real-engine tests. The browser runs two1800-second workers sequentially, each with180 bounded10-second chunks, whereas the synchronous fixture admits a much larger single advance. Worker overhead and repeated validation/context work must be measured before changing any readiness budget. Existing worker bounds, numerical assertions, exclusions and retry policy remain unchanged during diagnosis.

Raw CI artifact, full job log and summaries are retained under `artifacts/phase-4-20260911/ci-34568246585/`, `ci-live-log-attempt.txt` and `ci-initial-summary.json`. Local full acceptance498/498 and37/2 remains historical evidence for377ce40; it does not waive this failed CI gate. Any repair requires independent recheck and fresh final local/CI acceptance.

## Measured repair and independent recheck

The profile identified repeated rack/server construction when routine pump/controller references only needed canonical support assets. `49fe4f7` first resolves existing support equipment through the existing attachment-only generator, retaining full canonical fallback for rack/node/unmatched references. State validation uses a per-call support index and expands a module inventory once when actually needed. Direct asset precedence, all validation guards, valid asset identities/fields, numerical steps, work/chunk limits and120-second worker deadline are unchanged. Thirteen independent lookup cases cover all three existing families, standby variants, partial racks, valid fault/log references and malformed IDs.

`cbb6922` displays each actually completed and validated signature report while the next worker runs, with real committed-time progress and `aria-busy` retained until all execution finishes. No endpoint difference appears with only one card. The old pair notice is cleared at start. Testing now waits for the actual execution control to become ready within the **unchanged60-second whole-test budget**, then performs all existing12-second exact-value/export/error assertions. A ready control alone does not establish success. No timeout setting, retry, expected value or exclusion changed.

Independent Testing on immutable `cbb692236e1897b12dde9cb253bbd4484210e4cc`, tree `86ff7a5b1cc5f2439398dbb7d435c4f95c042e7b`:137/137 focused tests in9 files passed, zero failures/pending; build/typecheck passed. Two1800-second default-chunk worker tests validate every delivery and require exact complete experiment, physical module, event/log/cursor equality with the synchronous reference. They still use180 chunks per run. The immediate-scheduler Node observations were1288/1388 ms after repair versus2299/2178 ms before; these are timing observations with documented scope, not guarantees.

Fixing also retained exploratory native Chromium before/after profiles. Both results appeared at5875 versus4160 ms, with first completed card at5872 versus2329 ms. The after probe was labeled as a pending UI patch, so it is exploratory evidence rather than an immutable-candidate performance certificate. Exact-candidate Testing, full local acceptance and fresh CI remain the authoritative gates. See `CI-FIXING-RECEIPT.md` and the independent review.

Independent combined engineering review passed on that exactcbb6922 tree; `CI-ENGINEERING-REVIEW.md` preserves the review. The six fresh Chromium journeys passed with no failures/skips/retries/flaky results in20.418721 seconds; the signature journey took5057 ms. `CI-TESTING-RECEIPT.md` records the exact source and native evidence. These repaired focused results require a new complete frozen local gate and source CI before promotion.
