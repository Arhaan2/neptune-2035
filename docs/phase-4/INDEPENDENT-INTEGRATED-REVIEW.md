# Phase 4 integrated engineering review — findings outstanding

Reviewer `/root/verification`; immutable candidate `7311c421a924a435e8e83a635a1a39dc84efd6c6`, tree `27e9592de5c233ab63d92804c09f7299ba21b2fd`. Source read using `git show` in root's isolated clone. No production or test files changed and no duplicate broad suite run. This is **not an engineering or release PASS**; Root assigned the issues below to Fixing and independent regression coverage to Testing.

## Findings

| Local ID | Severity / requirement | Exact source and reproduction | Expected correction |
| --- | --- | --- | --- |
| V4-I01 | P1 — 4A continuous settling | `experiment/runtime.ts:44–46` samples post-step temperature predicates, then backdates a qualifying candidate to the interval start. Real small-design reproduction: obtain initial and one-second coolant from the canonical solver; choose the supported coolant limit at their midpoint, so initial coolant violates and the end boundary passes. Use one-second settling dwell, max warmup three seconds, permissive rate bounds and no controller-quiescence requirement. The current logic admits the initially violating first interval as healthy dwell. Testing was given this exact independent fixture; numeric receipt pending at checkpoint. | Honor the start thermal predicate for the complete accepted interval and validate the terminal predicate too. Earliest complete healthy interval is `[1,2)`, so earliest settlement is 2 s, not 1 s. Keep the existing post-controller T03 repair. |
| V4-I02 | P1 — 4C/4D unavailable counterfactual observations | `experiment/runner.ts:51` checks completed status and interval unavailable seconds, but not a null required terminal condition or `UNAVAILABLE` evaluation. Clone a completed real 8-accelerator pair, set one branch's `metrics.boundaryHealthy=null` and recompute evaluation. `evaluateExperiment` correctly returns UNAVAILABLE; the project validator accepts this explicitly supplied unavailable evidence, but comparison still publishes comparable and a numeric delta. Testing received this fixture for independent execution. | Incomplete required interval or terminal observations must suppress apparently valid paired incremental conclusions. Return incomplete/no delta. A completed FAIL with available evidence remains a valid comparable branch. |
| V4-I03 | P1 — 4C reference event and recovery timing | `metrics.ts` stores `referenceTimeS` as first qualifying violation onset but associates `referenceEventId` with an earlier fault. `ui/ExperimentPanel.tsx` prints that fault ID "at" the violation time. Independently decoded actual native-browser exported signature evidence: fault `signature-duty-trip` is declared at 30 s; first violation/referenceTime is 240 s; recovery onset 364 s, confirmation 369 s. Current displayed confirmation elapsed 129 s is relative to violation onset, while the UI presents it as the fault reference. | Preserve the persisted numeric meaning if desired, explicitly label violation onset 240 s and its elapsed durations 124/129 s; separately show actual fault time 30 s and event-relative onset/confirmation 334/339 s. Do not silently change `whole-run-1` semantics or misdate the fault. |
| V4-I04 | P1 — 4C fair UI comparison / 4D unavailable values | Root-raised concern independently confirmed: `TwinApp.tsx:1387–1408` unconditionally subtracts two terminal snapshots and labels both at the first run's physical clock. The new saved-experiment path at 531–535 explicitly preserves unequal durations. The cards at 1320–1322 fall back to each first module, but delta at 1392–1397 substitutes zero K for a missing selected module. Fixtures: compare saved 20 s and 40 s experiments; select a current module absent from one/both smaller saved designs. The UI can label unequal endpoints "at 20s" and fabricate zero or roughly ±300 K temperature differences. | Suppress unqualified deltas for unequal/incomplete windows or clearly identify both endpoints. Require an explicit common mapped module; represent missing observations as unavailable. The general explanatory paragraph does not correct the false time/zero measurement in the numeric line. |
| V4-I05 | P1 — 4D reproducible replay and 4E UI | Building/Root-raised concern independently confirmed: `TwinApp.tsx:1141` Replay and `:1170` Seek call `sim.replay(state.events,time)` without the persisted definition. `useTwin.ts` sends a definition only when explicitly supplied; otherwise the worker initializes an uninstrumented legacy state. Thus a normal control can discard the experiment definition and authoritative metrics. Testing owns native-browser reproduction. | Reconstruct the same declared experiment and recorded inputs with explicit evaluation/physical time handling, preserve start mode and step, and mark an incomplete seek observation accordingly. Legacy replay must continue retaining its existing compatibility behavior. |

These findings are based on inspected code paths; I03 also has directly inspected native exported numerical evidence. Root and Testing were asked to retain executed reproductions for the remaining targeted fixtures. Do not report static inspection as a reviewer-run test. Root owns canonical issue IDs and commit integration; local V4-I identifiers identify this review only.

## Consequential semantics that passed this bounded review

- The accepted-interval hook takes actual dt-positive electrical/network dispatch and per-module battery powers before boundary solves overwrite them. Thermal interval predicates use beginning temperatures for evaluation; terminal extrema are observed separately. Aggregate changes occur in the cloned numerical candidate and checkpoint validation binds metric coverage, physical step/time and definition identity. The warmup predicate exception is I01 above.
- Battery bus discharge and charge are separately integrated; module efficiencies produce modeled conversion loss. The storage identity is `net = charge - discharge - conversionLoss` in Wh, and the independent test includes discharge followed by recharge. Controller counts are appended at real controller log creation, independent of log eviction and UI message delivery.
- The fixed T02–T05 logic now prioritizes failed terminal recovery status, confirms zero dwell at the qualifying boundary without duplicate episode evidence, retains missing coolant/air coverage even alongside a known thermal failure, and performs settling confirmation after boundary controller transitions. The independent tests retain all four expectations, including export/resume across the thermal transition and withheld t=0 evaluation fault during failed warmup.
- Cold/settled definitions retain explicit workload, independent required demand, environment, numerical settings, and service/thermal/storage/controller settling predicates. Warmup time and physical state are preserved while evaluation metrics begin at a separate origin. The complete-current-state field values are not reset or batteries refilled at origin.
- Legacy schema-3 physical checkpoints without the extension stay uninstrumented after resume and show whole-run metrics unavailable. Existing schema-2/older solver compatibility behavior remains separate. New import validation bounds detailed evidence and numeric quantities, rejects unknown experiment semantics and recomputes outcome from stored lifecycle/aggregates. It explicitly labels imported aggregates supplied evidence, not independently verified measurements.
- Pair definitions retain nonfault workload/environment commands, stable input order, same physical initial-state identity, physical assumptions, criteria, duration and timestep. Interactive extra inputs reject controlled-pair status. Each supported settled pair is deterministic from matching cold initial state and identical unfaulted warmup settings. Available completed failures may correctly yield signed deltas; unavailable terminal handling needs I02.
- Exact aggregates are independent of bounded 600-sample trace and 1,000-entry detail. Retention keeps global extrema and exposes truncation. No per-accelerator/per-timestep history or new physical/network solver model was introduced.

## Independent fixture and evidence interpretation

The synthetic oracle is arithmetically independent of the simulator: `60*5 + 20*5 = 400 accelerator-seconds`; minimum 40 at 5 s; service violation union 10 s; healthy dwell `[15,20)` confirms at 20 s when its terminal condition remains healthy. It is not used as evidence of empirical physical validation.

Two corrected test-harness fixtures are legitimate: the extrema-only fixture now commits contiguous `[0,2)` and `[2,4)` intervals before observing those boundaries, preserving its original asset/value/time assertions; the repeated-interruption fixture moves the renewed outage from exactly the first confirmation at 20 s to 22 s so it actually tests an earlier confirmed recovery followed by a later interruption. The separate terminal-relapse test still requires cancellation at 20 s. Neither change weakens the behavioral guarantee or turns an actual production defect into an assertion adjustment.

Directly decoded `testing-phase4-browser-executed.json` attachment `phase4-signature-exported`:

| Quantity | No standby | One standby |
| --- | ---: | ---: |
| Evaluation duration | 1800 s | 1800 s |
| Unmet requirement | 79,360 accelerator-s | 0 accelerator-s |
| Service violation | 124 s | 0 s |
| Thermal violation | 80 s | 0 s |
| First violation | 240 s | None |
| Maximum coolant | 320.48382684974143 K at 303 s | 303.6718266831685 K at 38 s |
| Recovery onset / confirmation | 364 / 369 s | No qualifying interruption |

The service shortfall is independently consistent with `640*124=79,360`; observed restoration and controller response explain why fault at 30 s is not service interruption at 30 s. Both runs retain 1,280 final serviceable accelerators, while tests require final coolant difference below 0.01 K and air below 0.1 K. The real 100,000-accelerator scalable-network fixture's outage result `100,000*10=1,000,000 accelerator-seconds` matches its declared [5,15) disruption. No hardcoded result injection was found in the simulator or demonstrations.

Testing receipts report 102 focused core cases and 12 pair/signature/campus cases, with no failures at their named integrated trees. Those counts do not replace final full baseline suites, both actual telemetry integrations, ordinary three-engine browser gates, CI, accepted-build parity or hosted verification. Reported memory is process RSS/heap snapshots and process high-water RSS, explicitly not isolated allocation or a performance guarantee.

## Checkpoint

Engineering acceptance withheld until I01–I05 are repaired and independently rechecked. No other actionable blocker was found in the bounded inspected surfaces. Root may checkpoint this role while Fixing and Testing complete the handoff, then resume it on the repaired immutable candidate. Exact final-source CI, deployment, rollback and public-artifact reviews remain separate future gates.
