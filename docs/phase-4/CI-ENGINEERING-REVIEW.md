# Verification — combined CI repair engineering approval

**Engineering PASS** for immutable source `cbb692236e1897b12dde9cb253bbd4484210e4cc`, tree `86ff7a5b1cc5f2439398dbb7d435c4f95c042e7b`. No new blocking engineering finding in the combined repair. This is bounded source-and-focused-evidence approval. Fresh full local acceptance, successful exact-tree source CI, accepted-main artifact identity and hosted acceptance remain required; release approval is withheld until those gates pass.

## Scope and source assessment

Read the complete production diff from previously reviewed `377ce40c54479143da52146ef6283ddd3c1f3be8` to this candidate, both new regression diffs, readiness change, CI investigation documentation, Fixing receipt and native probes. No production files were written and no duplicate broad test suite was run by Verification.

1. `src/twin/assets/design.ts:217`: support lookups use the existing canonical attachment-only inventory first. Direct stored-asset precedence and exact ID lookup remain unchanged. Any unmatched reference falls back to the original complete inventory. This preserves rack/node resolution, partial-rack limits, absent standby rejection, specification-resolved fields and malformed-ID behavior; it does not introduce a second asset model.
2. `src/twin/persistence/state.ts:33`: support inventory is materialized lazily within each validation call; full inventory expansion occurs once per module if necessary. Both caches are local, preventing cross-design or post-mutation stale admission. Every pre-existing structure, key, identity, event, cursor, quantity, equipment, startup deadline and causal-history guard remains intact. No solver/model version, formula, grid, chunk, execution budget or schema change is required or made.
3. `src/ui/useTwin.ts:32` and `src/ui/TwinApp.tsx:510`: matching version/request/epoch worker progress is displayed as progress only. Result cards are appended only after a complete worker reply passes `validateState`. Copied result arrays prevent mutation of the published array. The stale prior-pair completion notice is replaced at start; `aria-busy` stays true while either signature job remains. One completed card preserves its absolute report; the existing length-two endpoint comparison guard and paired-role guard prevent partial-result deltas. Errors/resource limits still reject and remain visible. Both signature jobs remain sequential and unchanged.
4. `tests/browser/phase4.spec.ts`: the new readiness predicate waits for the actual signature action to re-enable after completion or failure, within the original60-second total test budget. The12-second value assertion limit, exact79360/0 values,1800-second coverage, fault/recovery times, final-state comparison, exports and error checks are unchanged. Readiness alone cannot pass the journey. No sleep, retry, skip, fabricated completion or fixture relaxation was added.

## Independently inspected evidence

The failed original CI evidence is preserved in `verification-ci-34568246585.md`: native merge identity matched the candidate tree,498unit checks passed and34browser journeys passed with2historical skips and3signature failures. All failures occurred while busy at the12-second first-card deadline, not at an observed differing numerical value. Independently decoded Chromium trace shows the second signature worker began8.107seconds after the first, proving actual first-run completion; Firefox/WebKit had only begun their first before the assertion ended. That failed gate remains historical evidence and is not waived by this receipt.

New canonical lookup regression13 cases and complete default-chunk worker regression2 cases both pass on the old production source, so they are preservation checks rather than fabricated pre-repair numerical failures. The lookup cases compare every canonical asset across generations1/2/3 with standby0/1, partial racks, support/rack/node faults/logs and invalid local references. Long-worker cases compare full experiment, modules, events, log and applied cursor to the reference and validate each delivered checkpoint.

Independently parsed `testing-ci-repair-focused.json`:137/137 passed, zero failed/pending,9files: lookup13, baseline1, definition23, demonstrations2, engine18, metrics32, pairing11, persistence17, worker20. Testing receipt binds its merge `a2d52591fd2f646b4cc885f7422302947f6e99c1` to the same candidate tree; Verification independently confirmed the tree hash. Production build/typecheck pass is recorded by Testing.

Independently parsed `testing-ci-repair-chromium.json`:6/6 expected, zero failures/skips/flaky, every retry0,20.418721seconds total. The genuine signature journey passed in5057ms, including the prior pair, exports and exact final histories. Native production assets recorded by Testing are `worker-BjFFDFG4.js` and `index-CfEDeDcj.js`; the build was fresh for the immutable candidate. This establishes the actual local Chromium journey, not Linux CI or hosted behavior.

Fixing's native exploratory probes show first card5872→2329ms and both cards5875→4160ms locally. The after-probe records one completed card, busy=true, no endpoint delta; final busy=false and shortfalls79360/0 at1800s. Its source label explicitly refers to pending incremental UI work; use it as development evidence, with the independent final candidate gate as immutable acceptance evidence. Immediate-scheduler Node measurements exclude browser scheduling; pre-repair whole-test and post-repair handler-only timings have different scopes and must not be advertised as an exact speedup ratio. No universal performance or physical-validation claim follows.

## Remaining gate

Root may freeze documentation and run complete mandatory local/CI gates on this repaired production tree. Any further substantive source change or failed mandatory gate requires targeted independent review. A passing local Chromium gate does not establish final release readiness or remove the requirement to verify Firefox/WebKit, native telemetry integrations, main/artifact identity and deployed Pages bytes.

Evidence: `testing-ci-signature-investigation.md`, `testing-ci-repair-focused.json`, `testing-ci-repair-chromium.json`, `testing-ci-repair-worker-observations.json`, `fixing-CI01-resolution.md`, `fixing-CI01-native-{baseline,after}.json`, and the preserved original CI artifact directory. Prior resolved I01–I06 and initial contract/footprint findings remain covered by the unchanged reviewed numerical implementation and the137-check focused recheck.
