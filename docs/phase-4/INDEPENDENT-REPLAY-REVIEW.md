# Independent repair review at e32e9a0

Reviewer `/root/verification`. Immutable candidate `e32e9a0626f5e8e24f8fb76fd1020244470191d2`, inspected through `git show`. **Engineering acceptance remains withheld** for the new initial replay-boundary defect below. No production or test modifications and no duplicate full suites.

## Prior finding resolutions

- **V4-I01 / warmup: PASS for the repair.** `runtime.ts` now uses interval-start temperatures when testing the warmup interval and retains the existing post-controller boundary confirmation. The independently written midpoint-temperature fixture requires initial violation, continued warming at 1 s and settlement at 2 s. Root/Testing report 28/28 focused engine/pair tests at `d856c69`; raw evidence `testing-numerical-verification-repaired.json`. This fixes the one-interval false dwell without changing physical dispatch or refilling storage.
- **V4-I02 / unavailable pair: PASS for the repair.** `comparePair` now requires full declared coverage and checks the canonical evaluation for UNAVAILABLE, including null terminal required conditions. The fixture survives project serialization/import as explicitly supplied unavailable evidence and then requires incomplete/no delta. Available completed FAIL remains comparable, as intended.
- **V4-I03 / reference clocks: PASS for source semantics, affected executed gate pending.** The report now preserves metrics' existing qualifying-violation `referenceTimeS`, identifies that basis, and separately derives actual fault time from recorded inputs relative to evaluation origin. Signature values are explicitly required to be fault 30 s, violation 240 s, onset 364 s, confirmation 369 s, event-relative onset 334 s and confirmation 339 s. UI names fault-relative and violation-relative quantities separately. No persisted `whole-run-1` meaning changes.
- **V4-I04 / endpoint delta: PASS for source semantics, affected executed gate pending.** Generic endpoint differences now require matching observation time basis, matching elapsed time and the selected module actually installed in both runs. No absent module is converted to zero K; cards also report missing values instead of silently substituting another module. Cross-design results remain explicitly separate absolute comparisons; controlled pair validity is still checked separately.
- **V4-I05 / Replay/Seek: incomplete repair.** Source now carries the experiment definition, original physical initial checkpoint, numerical step and requested physical seek time. Recorded interactive inputs explicitly derive a new definition with parent provenance; declared-only input replay retains the original definition. Those are the correct contracts, but the helper returns a pre-commit extension that the worker rejects, as below.

## V4-I06 — replay helper returns an uncommitted initial checkpoint

Severity **P1**, requirements 4D/4E. At the reviewed SHA, `replayExperimentState` in `experiment/runner.ts` clones `run.initialState`, calls `attachExperiment`, and returns immediately. `attachExperiment` deliberately stages an incomplete candidate: it sets evaluation to the placeholder `Evaluation has not completed.`, has not observed the initial boundary, and for a cold run admits events without applying any due at t=0. `initialize(design, definition)` normally completes this sequence with canonical `applyEvents` and `finishExperimentBoundary` before validating/returning.

The new UI passes this helper result as worker `request.state`. The existing worker validates supplied state before its later `advance(...,0)` admission call. Even an ordinary reference run with first fault at 5 s fails the stored-evaluation binding because `evaluateExperiment` generates the running/coverage reason rather than the helper's placeholder. A t=0 fault additionally violates the exact applied-event cursor. This prevents Replay/Seek from using the intended restored state.

Targeted independent reproduction supplied to Testing: complete the real reference experiment, call `replayExperimentState`, then validate the result and send it as the worker replay state. Expect a valid post-events/controller initial checkpoint and matching replayed definition/metrics. Also cover a t=0 fault, zero-duration definition, settled start, original initial-state preservation and recorded interactive inputs. A direct `advance` call on the staged helper state is not sufficient: `advance` itself validates before applying events. Fixing should return a canonically finalized initial checkpoint while retaining the original physical initial fields.

Testing/Fixing were notified immediately; this receipt records source proof and does not claim a reviewer-executed reproduction. New immutable repaired source and affected native/browser evidence are required before engineering PASS.

## Fractional battery fixture correction

Independent review approves the corrected arithmetic in `da66e52` / root `2906829`. Eight required accelerators over one second require 8 accelerator-seconds of service. For the supported 10 Wh UPS, full workload, optional networks and grid trip at 0, the pre-existing canonical IT-energy accumulator records `12000 W * 0.5 s / 3600 = 1.6666666667 Wh`, proving one 8-accelerator node served for the first half-second, then zero for the second. Served quantity is 4 accelerator-seconds and unmet quantity is therefore 4; service violation duration is 0.5 s starting at 0.5 s. The initial zero-duration display solve uses a conservative one-second feasibility horizon and reports zero; holding that display value would incorrectly produce shortfall 8.

The original proposed expectation that both halves were served was an unsupported harness assumption, not a production defect. Correction derives from the established physical energy accumulator and retains the test's crucial guarantee: actual subsecond dispatch, rather than the t=0 display snapshot, determines metrics. No implementation or physical formula was changed to make this test pass.

## Documentation factual review

`DEMONSTRATIONS.md` signature temperatures, interruption interval `[240,364)`, capacity arithmetic, 100,000-accelerator scalable-network/1 GW fixture, timing observations and process-memory caveat agree with inspected native exported evidence and the independent envelope receipt. `ACCEPTANCE.md` correctly keeps final/local/CI/public acceptance pending and distinguishes focused runs from final gates.

Two small factual edits were sent to Root: identify Verification as discovering the warmup/pair issues and Testing as executing their reproductions, and point footprint inspection to the actual `Disturbance footprint and experiment assumptions` disclosure rather than the full-report disclosure that contains definition/inputs/evidence but no footprint object. No source/Pages publication is asserted by this review.
